# new4 — E₁ 与 t₁ 的关系：从 so(5) 生成元与 Sp(2) 四元数表示严格推导

> 目标：**只使用我们文档已经建立的表示方法**（[model.md](./model.md) 的 so(5) 生成元
> $X_{ij}=i\gamma_i\gamma_j$ 与 Sp(2) 四元数分块、[new3.md](./new3.md) 的 Riccati 有效哈密顿量），
> 推出有效模型里 **E₁ 与 t₁ 之间能写下的、精确的代数/谱关系**。
> 全程**不从纳米线微观模型重新推导 E₁**（那是论文的输入，不是本文件的输出），
> 也不引用补充材料。所有结论都标注了推导来源：哪些是**表示法的必然结果**，
> 哪些是**论文的微观经验输入**、表示法本身推不出来。

---

## 0. 结论摘要（先给答案）

在只含 $\{\gamma_1,\gamma_2,\gamma_a,\gamma_b\}$ 四个活跃马约拉纳的子空间里，
$E_d\to0$（协议 Step-1 操作点）时，四个瞬时本征能量是

$$\boxed{\;\pm\sqrt{E_1^{\,2}+(|t_2|-|t_1|)^2}\;,\qquad
\pm\sqrt{E_1^{\,2}+(|t_2|+|t_1|)^2}\;}$$

这不是数值拟合，而是由两条**生成元对易/反对易事实**直接推出的精确结果：

1. **E₁ 的生成元 $X_{12}$ 与 t₁ 的生成元 $X_{b1}$ 反对易**：$\{X_{12},X_{b1}\}=0$，
   于是 E₁ 与 t₁ 在能谱里按 **勾股（正交合成）** 方式进入，而非相加。
2. 任意 $E_d$ 下完整谱为
   $$\pm\sqrt{(E_1+E_d)^2+(|t_2|-|t_1|)^2},\quad
   \pm\sqrt{(E_d-E_1)^2+(|t_2|+|t_1|)^2},$$
   由宇称算符 $\Pi=X_{12}X_{ab}=(-1)^{n_1+n_d}$ 将 $H$ 分成两个 $2\times2$ 块，
   每块都是 $a\,\sigma_z+b\,\sigma_x$ 形式、本征值 $\pm\sqrt{a^2+b^2}$（见 §3）。

**因此表示法给出的 E₁–t₁"关系"的确切内容是**：

- 这两个参数是 so(5) 里**两个不同生成元方向**的系数：$E_1$ 是 $X_{12}$
  （$i\gamma_1\gamma_2$，对角/码空间内 $\sigma_z$ 型），$t_1$ 是 $X_{b1}$
  （$i\gamma_b\gamma_1$，非对角/需经 ancilla 中转）。它们在 Sp(2) 分块里**位置不同**
  （E₁ 在 A、D 对角块的 $\mathbf i$ 分量；t₁ 在 B、C 非对角块的实部），见 §5。
- 由于 $\{X_{12},X_{b1}\}=0$，二者在**任意谱**中正交合成；且总能谱里 E₁ 永远与 $E_d$
  配对（$\pm\sqrt{(E_1\pm E_d)^2+\cdots}$），t₁ 永远与 $t_2$ 配对——**谱里不存在
  单独的 $E_1 t_1$ 交叉项**（因为反对易生成元的 $H^2$ 交叉项来自同板块配对，见 §3）。
- **表示法不强迫任何函数型 $t_1=f(E_1)$**。E₁、t₁ 是 $H_{EM}$（论文 Eq.(2)）的两个
  独立输入系数。论文所说的"t₁ closely related to E₁ / 同阶振荡 / E₁ 趋于零则 t₁ 也
  很小"是**微观共享起源**（两者都源于 γ₁ 与环境的杂化/重叠）的经验输入，
  不是代数必然。表示法给出的是：**一旦微观给出 t₁ 与 E₁ 的数值，它们如何共同决定
  谱与动力学**——即正交合成律与能隙条件（§4、§6）。

---

## 1. 哈密顿量在 so(5) 生成元基底中的嵌入

论文 Eq.(2) 的有效哈密顿量（见 [arts/pq.txt](./arts/pq.txt) 及 [model.md](./model.md) §2.2）
用 $X_{ij}=i\gamma_i\gamma_j$（10 个 so(5) 生成元）写成：

$$H_{EM}=E_1 X_{12}+|t_2| X_{a2}-|t_1| X_{b1}-|t_3| X_{a3}+E_d X_{ab},$$

其中记 $X_{12}=i\gamma_1\gamma_2$，$X_{ab}=i\gamma_a\gamma_b$，$X_{a2}=i\gamma_a\gamma_2$，
$X_{b1}=i\gamma_b\gamma_1$，$X_{a3}=i\gamma_a\gamma_3$。符号沿用 [model.md](./model.md)：
E₁、$|t_2|$、$E_d$ 带正号，$-|t_1|$、$-|t_3|$ 带负号。

> 记号说明：[model.md](./model.md) §2.2 用编号简写 $X_1,X_5,X_7,X_6,X_{10}$ 表示这 5 个
> 生成元（未显式列出编号↔下标对应）；本文件改用**反映马约拉纳下标的
> $X_{12},X_{a2},X_{b1},X_{a3},X_{ab}$**。二者与各项系数（$E_1,|t_2|,-|t_1|,-|t_3|,E_d$）
> 的配对关系，在 [model.md](./model.md) §3.5 的 A/B/C/D 块（$E_1\!\leftrightarrow\!\mathbf i$、
> $|t_2|\!\leftrightarrow\!\mathbf j$、$|t_1|\!\leftrightarrow\!\text{实部}$、
> $E_d\!\leftrightarrow\!\mathbf k$、$|t_3|\!\leftrightarrow\!\mathbf i$）与本文 §5 中完全一致。

**子空间截断（本文件的分析范围）**：把 $\gamma_3$ 相关的 $-|t_3|X_{a3}$ 项冻结/置于待
研究之外，只保留 $\{\gamma_1,\gamma_2,\gamma_a,\gamma_b\}$ 四个活跃马约拉纳。它们张成
一个双费米子（两个"对"：$(1,2)$ 与 $(a,b)$）的四维空间 $|n_1,n_d\rangle$。
于是本文件的哈密顿量是

$$\boxed{\;H=E_1 X_{12}+E_d X_{ab}+|t_2|X_{a2}-|t_1|X_{b1}.\;}$$

写清楚记号（避免与模型文档里的 $X_1,X_5,\dots$ 编号混淆），四个生成元：

| 生成元 | 定义 | 物理项 |
|---|---|---|
| $X_{12}$ | $i\gamma_1\gamma_2$ | $E_1$（码空间内 $\sigma_z$ 型杂化） |
| $X_{ab}$ | $i\gamma_a\gamma_b$ | $E_d$（ancilla 对的能量） |
| $X_{a2}$ | $i\gamma_a\gamma_2$ | $+|t_2|$（跨对耦合，D 步主角） |
| $X_{b1}$ | $i\gamma_b\gamma_1$ | $-|t_1|$（跨对耦合，spoiler） |

约定：下面出现 $t_1,t_2$ 处均为 $|t_1|,|t_2|$ 的大小（$\ge0$），符号已显式写进 H。

---

## 2. 生成元代数：对易/反对易表（全部由 $\{\gamma_i,\gamma_j\}=2\delta_{ij}$ 推出）

四个生成元都满足 $X^2=1$（因为对 $i\ne j$，$(i\gamma_i\gamma_j)^2=1$）。
其乘法关系只取决于两对下标是否共享马约拉纳：

**规则（可由 Clifford 关系逐项验证）**：两个生成元 $i\gamma_i\gamma_j,\;i\gamma_k\gamma_l$
——若 $\{i,j\}\cap\{k,l\}=\varnothing$（不相交对）则**对易**；若共享恰好一个指标则
**反对易**。

逐对列出：

| 对 | 共享指标 | 结果 | 备注 |
|---|---|---|---|
| $X_{12},X_{ab}$ | 无（$\{1,2\}\cap\{a,b\}=\varnothing$） | $[X_{12},X_{ab}]=0$ | 乘积记 $\Pi$ |
| $X_{a2},X_{b1}$ | 无（$\{a,2\}\cap\{b,1\}=\varnothing$） | $[X_{a2},X_{b1}]=0$ | 乘积也是 $\Pi$ |
| $X_{12},X_{a2}$ | 共享 2 | $\{X_{12},X_{a2}\}=0$ | — |
| $X_{12},X_{b1}$ | 共享 1 | $\{X_{12},X_{b1}\}=0$ | **E₁↔t₁ 反对易** |
| $X_{ab},X_{a2}$ | 共享 a | $\{X_{ab},X_{a2}\}=0$ | — |
| $X_{ab},X_{b1}$ | 共享 b | $\{X_{ab},X_{b1}\}=0$ | — |

**核心代数对象——宇称算符** $\Pi$：

$$X_{12}X_{ab}=(i\gamma_1\gamma_2)(i\gamma_a\gamma_b)=-\gamma_1\gamma_2\gamma_a\gamma_b
= (i\gamma_a\gamma_2)(i\gamma_b\gamma_1)=X_{a2}X_{b1}=:\Pi.$$

（两条乘积路径给出同一个 $\Pi$，因为两对都不相交、各自对易。）由不相交对的对易性，

$$\Pi^2=X_{12}^2X_{ab}^2=1,\qquad [X_{12},X_{ab}]=0,\qquad [X_{a2},X_{b1}]=0.$$

在 $|n_1,n_d\rangle$ 基底里 $X_{12}=2n_1-1$、$X_{ab}=2n_d-1$，故

$$\Pi=(2n_1-1)(2n_d-1)=(-1)^{n_1+n_d},$$

即 $\Pi$ 就是**总费米子宇称**。它把四维空间分成两个二维扇区：
偶宇称 $\{ |00\rangle, |11\rangle\}$（$\Pi=+1$）与奇宇称 $\{|10\rangle,|01\rangle\}$（$\Pi=-1$）。

**$\Pi$ 是守恒量** $[H,\Pi]=0$：因为 $H$ 的每一项都与 $\Pi$ 对易——
$[X_{12},\Pi]=[X_{ab},\Pi]=0$（块内），$[X_{a2},X_{12}X_{ab}]=0$、
$[X_{b1},X_{12}X_{ab}]=0$（把 $X_{a2}$ 穿过 $X_{ab}$ 换一次得 $-1$，穿过 $X_{12}$
再换一次得 $+1$；$X_{b1}$ 同理，穿过 $X_{ab}$、$X_{12}$ 各换两次得 $+1$）。

> **表示法视角下的第一句话**：$H$ 不是 so(5) 的某个不可约分解的"单块"，而是被宇称
> 量子数 $(-1)^{n_1+n_d}$ 分成两块。这与 [model.md](./model.md) §2.4 的结论一致：
> 因为 10 维 $so(5)$ 闭包是满的、没有可用的更小不变子代数，所以协议中段（Step 1/2）
> 一般不存在把两段拼接成闭式的捷径。但**瞬时谱**仍可闭式求，靠的就是上面的
> $\{X_{12},X_{b1}\}=0$ 与 $\Pi$ 结构。

---

## 3. 谱的严格推导：$H^2=\text{标量}+\Pi\text{系数}$

把 $H$ 拆成"纵向"与"横向"两块：

$$V=E_1X_{12}+E_dX_{ab},\qquad W=|t_2|X_{a2}-|t_1|X_{b1}.$$

由 §2 反对易表：$V$ 的每一项（$X_{12},X_{ab}$）都与 $W$ 的每一项
（$X_{a2},X_{b1}$）反对易 ⇒

$$\{V,W\}=0\;\Longrightarrow\; H^2=(V+W)^2=V^2+W^2.$$

分别计算（利用各 $X^2=1$ 与块内对易性）：

$$V^2=E_1^2+E_d^2+E_1E_d(X_{12}X_{ab}+X_{ab}X_{12})
=(E_1^2+E_d^2)\cdot1+2E_1E_d\,\Pi,$$

$$W^2=|t_2|^2+|t_1|^2-|t_1||t_2|(X_{b1}X_{a2}+X_{a2}X_{b1})
=(|t_2|^2+|t_1|^2)\cdot1-2|t_1||t_2|\,\Pi.$$

合并：

$$\boxed{\;H^2=\underbrace{(E_1^2+E_d^2+|t_1|^2+|t_2|^2)}_{\text{标量，四个方向正交范数}}
+\underbrace{2\,(E_1E_d-|t_1||t_2|)}_{\text{交叉项}}\,\Pi.\;}$$

**关键观察**：交叉项只含 $E_1E_d$ 与 $|t_1||t_2|$——它们是**各自板块内的配对**。
不存在 $E_1|t_1|$ 项，这正是 $\{X_{12},X_{b1}\}=0$ 的直接后果：
反对易生成元的 $H^2$ 里，交叉贡献来自**与自身板块内另一生成元**的乘积，而不是跨
E₁/t₁ 方向。**这就是"E₁ 与 t₁ 互不干扰地正交合成"的代数起源。**

现在在 $\Pi=+1$ 扇区（偶，$\{|00\rangle,|11\rangle\}$）与 $\Pi=-1$ 扇区（奇，
$\{|10\rangle,|01\rangle\}$）内，$H^2$ 都是纯标量 × 单位矩阵。由于每个扇区内 $H$ 无迹
（下面矩阵会显示对角元互为相反数），每扇区两个本征值必为
$\pm\sqrt{(\text{扇区内 }H^2\text{ 的标量值})}$。分别合并平方：

$$\Pi=+1\ (\text{偶}):\quad
E_1^2+E_d^2+|t_1|^2+|t_2|^2+2(E_1E_d-|t_1||t_2|)
=(E_1+E_d)^2+(|t_2|-|t_1|)^2,$$

$$\Pi=-1\ (\text{奇}):\quad
E_1^2+E_d^2+|t_1|^2+|t_2|^2-2(E_1E_d-|t_1||t_2|)
=(E_d-E_1)^2+(|t_2|+|t_1|)^2.$$

于是**完整闭式谱**：

$$\boxed{\;\Pi=+1\ (\text{偶}):\quad \mathcal E=\pm\sqrt{(E_1+E_d)^2+(|t_2|-|t_1|)^2}\;}$$
$$\boxed{\;\Pi=-1\ (\text{奇}):\quad \mathcal E=\pm\sqrt{(E_d-E_1)^2+(|t_2|+|t_1|)^2}\;}$$

> 推导脉络：$H={\sum}h_{ij}X_{ij}$（so(5) 生成元线性组合，[model.md](./model.md) §2.2）
> → 用 Clifford 反对易关系建立 §2 对易/反对易表 → $\{V,W\}=0$ → $H^2=V^2+W^2$ →
> 宇称 $\Pi$ 把 $H^2$ 变成扇区内标量 → 开平方。**每一步都是代数/表示论，不是数值对角化。**

---

## 4. 显式矩阵与扇区结构（符号验证）

在 $|n_1,n_d\rangle$ 基底（$n_1$：$\gamma_1\gamma_2$ 对，$n_d$：$\gamma_a\gamma_b$ 对）下，
$H$ 的 $4\times4$ 矩阵可写成两副对角块（用 $t_1=|t_1|,\ t_2=|t_2|$）：

$$H=\begin{pmatrix}
-(E_1+E_d) & 0 & 0 & t_2-t_1\\
0 & E_1-E_d & t_1+t_2 & 0\\
0 & t_1+t_2 & -(E_1-E_d) & 0\\
t_2-t_1 & 0 & 0 & E_1+E_d
\end{pmatrix},$$

行序 $|00\rangle,|10\rangle,|01\rangle,|11\rangle$。可以看出：

- **偶扇区** $\{|00\rangle,|11\rangle\}$：
  $H_{\rm even}=\begin{pmatrix}-(E_1+E_d)&t_2-t_1\\ t_2-t_1&E_1+E_d\end{pmatrix}
  =-(E_1+E_d)\,\sigma_z+(t_2-t_1)\,\sigma_x$，
  本征值 $\pm\sqrt{(E_1+E_d)^2+(t_2-t_1)^2}$。
- **奇扇区** $\{|10\rangle,|01\rangle\}$：
  $H_{\rm odd}=\begin{pmatrix}E_1-E_d&t_1+t_2\\ t_1+t_2&-(E_1-E_d)\end{pmatrix}
  =(E_1-E_d)\,\sigma_z+(t_1+t_2)\,\sigma_x$，
  本征值 $\pm\sqrt{(E_d-E_1)^2+(t_1+t_2)^2}$。

每块都是 $(纵向)\sigma_z+(横向)\sigma_x$ 的"单个自旋 1/2 在磁场里"，磁场长度就是能隙。
E₁ 只贡献**纵向**（$\sigma_z$，码空间内），t₁ 只贡献**横向**（$\sigma_x$，跨对）；
纵向磁场的两个来源 $E_1,E_d$ 在偶块相加、奇块相减；横向磁场的两个来源 $t_1,t_2$
在偶块相减、奇块相加。**这正是 E₁、t₁ 各自角色最直接的读出。**

（此矩阵与全部闭式已用符号计算逐元素验证，且与 §6 数值表一致到机器精度。）

---

## 5. Sp(2) 四元数表示里的位置：E₁ 在 A,D，t₁ 在 B,C

按 [model.md](./model.md) §3.5 的四元数分块（$K=[[A,B],[C,D]]$，$\dot U=KU$），
各活跃生成元对应：

$$A(t)=\frac{E_1+|t_3|}{2}\,\mathbf i+\frac{|t_2|}{2}\,\mathbf j,\qquad
D(t)=\frac{-E_1+|t_3|}{2}\,\mathbf i+\frac{|t_2|}{2}\,\mathbf j,$$
$$B(t)=\frac{|t_1|}{2}+\frac{E_d}{2}\,\mathbf k,\qquad
C(t)=-\frac{|t_1|}{2}+\frac{E_d}{2}\,\mathbf k.$$

读出的 E₁ 与 t₁：

$$\boxed{\;A-D=E_1\,\mathbf i\quad(\text{E₁ 在对角块，}i\text{ 分量，}A,D\text{ 反号})\;}
\qquad\qquad
\boxed{\;B-C=|t_1|\quad(\text{t₁ 在非对角块，实部，}B,C\text{ 反号})\;}$$

- **E₁ 在对角块 A、D 的 $\mathbf i$ 分量**：这正是码空间内 $\sigma_z$-型方向
  $\Sigma_{12}=\operatorname{diag}(\mathbf i/2,-\mathbf i/2)$。它**直接**作用于码空间，
  **不经过 ancilla**。若 ancilla 完全空（Riccati 变量 $q\approx0$），有效哈密顿量
  $H_{\rm eff}=i(A+Bq)\approx iA$ 里 E₁ 以第一阶出现。
- **t₁ 在非对角块 B、C 的实部**：它是 $\Sigma_{15}$ 型的跨块耦合，**必须经过 Riccati
  变量 $q$**（$H_{\rm eff}=i(A+Bq)$ 中的 $Bq$）才能抵达码空间。也就是说 t₁ 的作用是
  "经 ancilla 中转"的，是第二阶的间接通道。

**分工（表示法的必然结构）**：E₁ 是码空间的"内场"（对角、直接、$\sigma_z$），
t₁ 是"经 ancilla 的外场"（非对角、间接、$\sigma_x$ 进入码空间）。两者在 $Sp(2)$
分块里物理位置不同，所以在任何演化中扮演的角色不同——即使它们在微观上数值相关。

> 也解释了为何"纯几何编织"会被 E₁、t₁ 同时污染（[model.md](./model.md) §7、
> [new3.md](./new3.md)）：E₁ 的 $E_{1,\rm eff}\sigma_z$ 与 t₁ 的 $t_{1,\rm eff}\sigma_y$
> 在码空间**不对易**（论文 pq.txt Sec.II 明确写 "noncommuting nature of $E_{1,\rm eff}
> \sigma_z$ and $t_{1,\rm eff}\sigma_y$"）。在生成元层面，这个不对易正是
> $\{X_{12},X_{b1}\}=0$——E₁ 与 t₁ 的生成元方向不同，它们在码空间的投影
> $E_1^\mathrm{code}$ 与 $t_1^\mathrm{code}$ 于是作为 $\sigma_z,\sigma_x$（或 $\sigma_y$）
> 两轴正交共存，导致波函数随时间振荡而非衰减，或随它们的比值改变旋转轴。

---

## 6. E₁–t₁ 关系的确切内容（含能隙条件与测量读出）

### 6.1 表示法能推出的（精确、无条件）

**(a) 正交合成律（§3）**：E₁ 与 t₁ 的生成元反对易，故二者在一切谱/能隙中按勾股合成，
永不出现在同一线性项里。完整谱是

$$\mathcal E=\pm\sqrt{(E_1\pm E_d)^2+(|t_2|\mp|t_1|)^2}\quad(\text{上}+E_d\text{ 对偶块，下}-E_d\text{ 对奇块}).$$

**(b) 分工（§5）**：E₁ 在对角块（码空间内、直接、$\sigma_z$），t₁ 在非对角块
（经 ancilla、间接、$\sigma_x$）。这解释了为什么两者在动力学中的可观测效应不同。

**(c) 能隙（绝热性）**：基态能隙是上述两组里最小正值。$E_d=0$ 时
$\Delta=\sqrt{E_1^2+(|t_2|-|t_1|)^2}$（偶块）。**若 $|t_1|\to|t_2|$，能隙 → $E_1$**：
即 $\gamma_1\gamma_2$ 杂化 E₁ 直接垫底，堵死了"靠 $t_1=t_2$ 让能隙归零"的可能性——
这正是论文所说的 E₁ 使编织不再是纯几何（见 [model.md](./model.md)、[arts/pq.txt](./arts/pq.txt)）。

**(d) 谱学读出 t₁（若已知 E₁、t₂）**：在 $E_d=0$ 时测量奇偶两块的能隙
$\Delta_\mp=\sqrt{E_1^2+(t_2\mp t_1)^2}$，则

$$\Delta_+^2-\Delta_-^2=4\,t_1 t_2\;\Longrightarrow\;
\boxed{\;|t_1|=\frac{\Delta_+^2-\Delta_-^2}{4\,t_2}.\;}$$

（$\Delta_+$ 为 $(t_2+t_1)$ 那个块的能隙，$\Delta_-$ 为 $(t_2-t_1)$ 块的。）
这是表示法给出的、**可由能谱直接反解出 t₁ 的解析公式**，只要知道 E₁ 与 $t_2$
（两者都能由别的测量/输入给定）。反过来，若已知 $t_1$ 与 $t_2$，可反解
$|E_1|=\sqrt{\tfrac12(\Delta_+^2+\Delta_-^2)-t_1^2-t_2^2}$。

### 6.2 表示法**不能**推出的（诚实边界）

- **不存在函数型 $t_1=f(E_1)$**。E₁、t₁ 是论文 Eq.(2) 里两个独立输入系数，对应
  so(5) 里两个独立生成元 $X_{12},X_{b1}$ 的系数。代数上没有把二者绑定的恒等式。
- 论文中的经验关系——"t₁ is closely related to E₁…oscillate with almost the same
  order of oscillation period""if $E_1$ remains stable around zero, $t_1$ will also be
  very small"（[arts/pq.txt](./arts/pq.txt) 引言与 Appendix C）——是**微观共享起源**
  的观测结论：两者同源于 γ₁ 与环境的耦合（同一个纳米线/结区杂化），因此随门电压等
  同步变化。这条**不是表示法的推论**，而是表示法之外的输入。
- 若要**从第一性原理验证/推导** $t_1$ 随 $E_1$ 的同步性，需要微观模型（如我们的
  [e1t1_relation/tb_wire.py](./e1t1_relation/tb_wire.py) 紧束缚路线）——那是另一个
  层面的工作，不在本文件表示法推导的范围内。

**一句话**：表示法把 E₁–t₁ 关系变成了**精确的代数对象**——它们按 $\{X_{12},X_{b1}\}=0$
正交合成、分居 Sp(2) 对角/非对角块、在总能谱中各自与 $E_d$/$t_2$ 配对、并给出能隙与
谱学读出公式；而"它们数值上同源同步"属于微观输入，表示法本身不产生 $t_1=\kappa E_1$
式的等式。

---

## 7. 数值验证（仅作校验，推导本身是纯代数的）

在 $|n_1,n_d\rangle$ 表示中直接构造 Hermite 的 $4\times4$ $H$，对角化并与 §3 闭式对照
（抽样，$E_1\in\{0.005,0.1,0.3\}$，$E_d\in\{0,0.3\}$，$t_1\in\{0.01,0.15\}$，
$t_2\in\{0.3,0.5\}$，共 24 组）：

```
 E1     Ed    t1     t2   | numeric eigenvalues      | analytic (closed form)     maxerr
0.005 0.000 0.010 0.300 | [-0.3100 -0.2900 0.2900 0.3100] | sqrt form (§3)        5.6e-17
0.005 0.000 0.150 0.300 | [-0.4500 -0.1501 0.1501 0.4500] | ...                   5.6e-17
0.300 0.000 0.010 0.300 | [-0.4314 -0.4173 0.4173 0.4314] | ...                   0.0e+00
0.300 0.300 0.150 0.500 | [-0.6946 -0.6500 0.6500 0.6946] | ...                   0.0e+00
```
（24/24 组全部吻合，最大误差 $<1.2\times10^{-16}$；偶数块/奇数块分别验证
$\pm\sqrt{(E_1+E_d)^2+(t_2-t_1)^2}$ 与 $\pm\sqrt{(E_d-E_1)^2+(t_2+t_1)^2}$。）

验算示例：$E_1=0.3,E_d=0.3,t_1=0.15,t_2=0.3$ →
偶块 $\sqrt{(0.6)^2+(0.15)^2}=\sqrt{0.3825}=0.61847$，
奇块 $\sqrt{0^2+(0.45)^2}=0.45$，与表中 $\pm0.6185,\pm0.45$ 一致。

---

## 附：与论文符号的一致性说明

- [model.md](./model.md) 与本文用 $X_{12}=i\gamma_1\gamma_2$；论文 pq.txt 正文在讨论
  $E_{1,\rm eff}\sigma_z$ 时采用 $\sigma_z=-i\gamma_1\gamma_2$（相差一个整体符号）。
  由于能谱与正交合成对整体符号不变，这不影响 §3–§6 的任何结论；仅在把"$\sigma_z$ 型"
  逐字对应到论文的 $E_{1,\rm eff}\sigma_z$ 时注意这个符号约定差异。
- 本文的 $E_d$ 项在偶块以 $(E_1+E_d)$、奇块以 $(E_d-E_1)$ 进入：$E_d$ 是 ancilla 对的
  能量，奇偶块符号差异来自 $\Pi$ 在本征态上的取法，属正常物理解读。
- 本文分析子空间为 $\{\gamma_1,\gamma_2,\gamma_a,\gamma_b\}$（冻结 $\gamma_3$/$t_3$ 项）。
  完整 5-马约拉纳 so(5) 的协议动力学见 [model.md](./model.md)；本文的贡献是把
  **E₁–t₁ 关系**在冻结 $t_3$ 的瞬时谱层面做成严格的代数闭式。
