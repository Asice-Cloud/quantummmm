# PRB111 严格推导总结与下一步

这份文档只做两件事：

1. 总结我们目前已经严格确认的结果；
2. 在这些结果上继续推理下一步应该怎么走。

## 1. 我们已经确认了什么

### 1.1 全空间主线是 `so(5) ≅ sp(2)`

论文的有效 Majorana 体系可以严格写成五个 Majorana 生成的 `so(5)` 线性闭包；等价地，也可以写成 `sp(2)` 的四元数矩阵表示。

这意味着：

- 全局模型不是单一 `su(2)`；
- 单位四元数不是默认全局结构；
- `Sp(2)` 是保留全部自由度的主框架。

### 1.2 Step 1 / Step 2 / Step 3 的层级不同

我们已经逐段确认：

- Step 1 的最小闭包一般到 `so(4)`；
- Step 2 会涨到完整 `so(5)`；
- Step 3 才能拆成 `u(1) ⊕ su(2)`。

因此：

- Step 1、Step 2 不能像 Step 3 那样直接压成单一单位四元数闭式；
- Step 3 可以先剥离一个对易的 `u(1)` 相位因子，再对剩余 `su(2)` 用四元数 / Euler / Wei–Norman 处理。

### 1.3 论文门控函数已经进入可计算形式

我们已经把论文中的分段门控写成了明确的时间函数，并在 Step 3 上进行了数值积分。

结果说明：

- Step 3 的 ODE 是可解的；
- 但低阶 Magnus 近似在当前参数下误差不小；
- 因此当前门控下，Step 3 虽然可解析化，但不属于“简单固定轴”那类闭式可积情形。

### 1.4 `Sp(2)` 四元数矩阵路线比“硬凑单位四元数”更严格

我们已经确认：

- 四元数可以作为 `Sp(2)` 表示的自然语言；
- 但只有在真正闭到 `su(2)` 的子块上，单位四元数才是合法的局部参数化；
- 对整段协议强行使用单位四元数，会丢失 Step 1 / Step 2 的真实非阿贝尔结构。

---

## 2. 这意味着什么

### 2.1 我们已经把“模型”做严了

严格模型已经建立为：从 Clifford / Majorana 的全空间出发，经由 `so(5) ≅ sp(2)` 的等价表示，再进入分段演化。

这条链条没有依赖先验的 reduced qubit 假设，也没有把全空间先投影到单一二维子空间。

### 2.2 但还没有得到整段协议的全局闭式解
当前缺失的不是模型本身，而是整个三段协议的可积性条件。

我们现在拥有的是：

- 全空间的严格线性结构；
- Step 3 的局部可解析化；
- 数值验证和 Magnus 近似作为辅助。

我们还没有得到的是：

- Step 1 / Step 2 的进一步低维可积化；
- 三段协议拼接后的单个初等闭式表达。

---

## 3. 继续推理下一步

### 3.1 下一步的逻辑起点

如果要进一步逼近“整篇论文的解析解”，最自然的起点不是再压缩表示，而是判断：

1. Step 1 是否还能进一步分成两个可独立处理的子块；
2. Step 2 是否存在隐藏的对易结构或可积轨道；
3. 三段协议能否通过分段 Magnus / 分段 Wei–Norman 统一拼接。

### 3.2 对当前论文门控的判断

从已经得到的结果看：

- Step 1 只有 `so(4)` 层级，不是单一 `su(2)`；
- Step 2 直接涨到 `so(5)`，说明它通常不会再降到简单闭式；
- Step 3 是唯一真正能做 `u(1) ⊕ su(2)` 分离的段。

因此，整段协议最现实的解析策略是：Step 1 / Step 2 保留 `\mathcal{T}\exp\!\left(\int K^{(s)}(t)dt\right)`，Step 3 做 `u(1) ⊕ su(2)` 分解。

### 3.3 更具体的下一步

如果继续往下推，优先级应该是：

#### 路线 A：把 Step 1 / Step 2 继续留在 `Sp(2)`，做分段数值或分段 Magnus

这是最稳妥的全空间路线。它不会额外引入不必要的假设，也能保持与论文门控的严格一致。

#### 路线 B：把 Step 3 的 `su(2)` 子块单独写成更标准的旋转参数

如果需要一个更像“解析解”的表达，Step 3 可以进一步改写为：

- 单位四元数旋转；
- 或 Euler 角；
- 或更适合数值积分的 Wei–Norman 标量方程。

#### 路线 C：寻找是否存在更强的特殊化门控

如果把论文的时间函数进一步特殊化成固定轴、单通道或近似常数，就可能得到真正闭式。

但这一步已经属于“额外假设”，不再是论文原始门控的严格结果。

### 3.4 论文变量对照表：哪些段能解析，哪些段只能保留时间有序指数

下表直接按论文中的门控变量来判断。这里的“可解析”指的是：能否在不额外改变论文门控的前提下，进一步降到可写成局部闭式或标准低维 ODE 的形式。

| 段落 | 论文变量设计 | 主要代数层级 | 目前可否直接给出闭式 | 该保留的表达 |
|---|---|---|---|---|
| Step 1 | `t2(t)` 从 0 升到 `tc`，`Ed(t)` 从 `E0` 降到 0；`t1(t)` 若保留则只作为辅助通道出现 | `so(4)` 级别的闭包 | 不能直接压成单一单位四元数；若继续降阶，最多先做 `so(4) ≅ su(2) ⊕ su(2)` 的结构分析 | 保留 `Sp(2)` 分段时间有序指数；必要时再做 `so(4)` 子块分解 |
| Step 2 | `t3(t)` 从 0 升到 `tc`，`t2(t)` 退去，`G1` 再切换，`Ed(t)` 处于零附近 | 完整 `so(5)` / `Sp(2)` | 不能给出单一低维闭式；时间依赖方向继续变化，低阶 Magnus 也不够 | 必须保留完整 `\mathcal{T}\exp\!\left(\int K^{(2)}(t)dt\right)` |
| Step 3 | `t3(t)` 从 `tc` 退到 0，`Ed(t)` 从 0 回到 `E0`；其余通道回到关闭/对易部分 | `u(1) ⊕ su(2)` | 可以：先剥离 `u(1)` 相位，再对 `su(2)` 子块写单位四元数 / Euler / Wei–Norman | `e^{\Phi(t)X_{12}} V(t)`，其中 `V(t)` 由 `su(2)` ODE 描述 |

从这张表可以直接看出：

- 论文原始门控里，真正适合做局部闭式的是 Step 3；
- Step 1 最多先看作 `so(4)` 结构，再谈可能的二次分解；
- Step 2 则应当坚持保留全 `Sp(2)` 的时间有序指数。

---

## 4. 当前最合理的结论

我们现在可以给出一个非常清楚的判断：

- **严格模型已经有了**：`so(5) ≅ sp(2)` + 分段门控 + Step 3 的 `u(1) ⊕ su(2)`。
- **解析化已经做了一半**：Step 3 可写成四元数 / Euler / Wei–Norman。
- **全局闭式还差一步**：Step 1 / Step 2 还没有进一步的可积结构，因此整段协议目前只能保留分段 `Sp(2)` 演化或做分段近似。

所以，下一步最值得做的不是继续强行压缩全模型，而是：

1. 固化 Step 1 / Step 2 的 `Sp(2)` 分段表达；
2. 对 Step 3 的 `su(2)` 方程做更标准的解析参数化；
3. 如果你要真正的闭式，再去找是否存在可接受的额外特殊化门控。

---

## 5. 直接可执行的下一步：先 Step 1，再 Step 2

下面给出不再绕弯的操作顺序。这里的目标不是再找一张更漂亮的表，而是直接判断哪一步还能继续降阶，哪一步必须换工具。

### 5.1 Step 1：先试 `so(4) ≅ su(2) ⊕ su(2)` 的自对偶分解

Step 1 的关键不是继续猜测它“像不像四元数”，而是把它写进一个明确的 `so(4)` 子代数里，然后做标准的自对偶/反自对偶分解。

具体做法是：

1. 从 Step 1 的哈密顿量中提取实际起作用的生成元，把它们重写成一个 4 维指标集合上的 `so(4)` 形式。
2. 引入标准的自对偶和反自对偶基底

\[
J_1^{\pm}=\frac12(L_{23}\pm L_{14}),\qquad
J_2^{\pm}=\frac12(L_{31}\pm L_{24}),\qquad
J_3^{\pm}=\frac12(L_{12}\pm L_{34}).
\]

3. 将 Step 1 的生成元展开成

\[
H^{(1)}(t)=\sum_{a=1}^3 \alpha_a^+(t)J_a^+ + \sum_{a=1}^3 \alpha_a^-(t)J_a^-.
\]

4. 检查是否存在一整组三个系数恒等为零：
	- 若 `\alpha_a^-(t)\equiv 0`，则 Step 1 落在一个 `su(2)` 子块里；
	- 若 `\alpha_a^+(t)\equiv 0`，则落在另一个 `su(2)` 子块里；
	- 若两边都不消失，则只能保留 `Spin(4)\cong SU(2)\times SU(2)` 的双块结构。

这一步的意义很直接：如果 Step 1 真的只活在一个子 `su(2)` 里，那就可以像 Step 3 一样写成单位四元数或 Euler 参数；如果不是，就不要再继续硬压。

### 5.2 Step 2：不再继续拆代数，直接做 commutant 和 Magnus 判定

Step 2 进入完整 `so(5)` / `Sp(2)` 后，继续做李代数直和分解的收益已经很低。这里应该改成“是否存在额外不变子结构”的检查。

具体做法是：

1. 把 Step 2 的哈密顿量写成固定基底下的线性组合 `H^{(2)}(t)=\sum_a h_a^{(2)}(t)B_a`。
2. 取一个时间无关的候选对称生成元 `X`，解线性方程组

\[
[X,H^{(2)}(t)]=0
\]

	是否对所有 `t` 都成立。
3. 若只有平凡解 `X\propto I`，则说明 Step 2 没有额外可分块的对称性，必须保留完整 `\mathcal T\exp`。
4. 若存在非平凡 commutant，则以它为基准做块对角化，再重新判断是否能降到更小的子代数。

对论文这组门控而言，Step 2 目前最现实的处理是：

- 保留 `Sp(2)` 分段时间有序指数；
- 或者在短时间段上做 Magnus 展开；
- 或者先转到 interaction picture，剥离明显的对易项，例如 `E_1` 或 `E_d` 对应的静态通道。

### 5.3 如果 Step 1 失败、Step 2 也没有 commutant，就直接换工具

如果 Step 1 的自对偶分解没有把它压进单个 `su(2)`，而 Step 2 的 commutant 检查又是平凡的，那么继续做李代数分解就不是最优路线了。

这时应该直接切换到下面三种工具中的一个：

- **分段 Magnus**：适合保留解析结构，又不要求精确闭式；
- **interaction picture / gauge 变换**：适合先剥离显式对易项，再看剩余非阿贝尔部分；
- **数值传播**：作为最后验证，检查近似是否靠谱。

也就是说，真正的流程不是“无限分解”，而是：Step 1 先试 `so(4)→su(2)⊕su(2)`，Step 2 先做 commutant test，不行就直接切到 Magnus 或数值传播。

---

## 6. 具体尝试结果：Step 1 / Step 2 的实际代数判定

我已经用论文的标准 `5\times 5` so(5) 表示，把 Step 1 / Step 2 的实际生成元集合做了一次直接闭包检查，结果很清楚。

### 6.1 Step 1 的结果

Step 1 的 active block 只作用在四个 Majorana `\{\gamma_1,\gamma_2,\gamma_a,\gamma_b\}` 上，`\gamma_3` 是旁观的 spectator。对应的李代数闭包维数是

\[
\dim \mathfrak g_{\mathrm{Step\,1}} = 6.
\]

这就是一个标准的 `so(4)`，而不是更大的 `so(5)`。

同时，在线性矩阵意义下的 commutant 维数是

\[
\dim \mathrm{Comm}(\mathfrak g_{\mathrm{Step\,1}})=2.
\]

这说明 Step 1 的确存在一个明显的块结构：一个 4 维 active block，加上一个 1 维 spectator 方向。

但这并不自动意味着它能压成“单一的一个 `su(2)`”。原因是 Step 1 的哈密顿量在这个 `so(4)` 里面通常同时激发了两个 `su(2)` 因子。也就是说，Step 1 最多先降到 `Spin(4)\cong SU(2)\times SU(2)`，是否还能再降成单一 `SU(2)`，要额外检查是否存在特殊的系数关系；对论文原始门控，这种关系一般并不成立。

### 6.2 Step 2 的结果

Step 2 把 `\gamma_3` 也拉进来之后，闭包直接涨满到

\[
\dim \mathfrak g_{\mathrm{Step\,2}} = 10,
\]

也就是完整的 `so(5)` / `Sp(2)`。

对应的 commutant 维数只有

\[
\dim \mathrm{Comm}(\mathfrak g_{\mathrm{Step\,2}})=1,
\]

这基本就是平凡标量解，说明 Step 2 没有可用于进一步块对角化的额外不变子结构。

### 6.3 直接结论

这次实际尝试给出的结论是：

- Step 1 真的值得继续试 `so(4) \to su(2) \oplus su(2)`，但大概率不会自动掉成单一 `su(2)`；
- Step 2 已经没有继续代数拆分的空间，应该直接切换到 `Sp(2)`、Magnus、interaction picture 或数值传播。

### 6.4 Step 1 的自/反自对偶展开

把 Step 1 的六维闭包看成作用在四个指标 `\{1,2,a,b\}` 上的 `so(4)`，取标准的自对偶/反自对偶基

$$
J_1^{\pm}=\frac12(X_{1b}\mp X_{a2}),\qquad
J_2^{\pm}=\frac12(X_{2b}\mp X_{1a}),\qquad
J_3^{\pm}=\frac12(X_{12}\pm X_{ab}).
$$

于是

$$
X_{a2}=-(J_1^+ + J_1^-),\qquad X_{1b}=J_1^+ - J_1^-,
$$

$$
X_{12}=J_3^+ + J_3^-,\qquad X_{ab}=J_3^+ - J_3^-.
$$

若写成

$$
H^{(1)}(t)=c_{12}(t)X_{12}+c_{a2}(t)X_{a2}+c_{1b}(t)X_{1b}+c_{ab}(t)X_{ab},
$$

则

$$
H^{(1)}(t)=
(c_{1b}-c_{a2})J_1^+
-(c_{1b}+c_{a2})J_1^-
+(c_{12}+c_{ab})J_3^+
+(c_{12}-c_{ab})J_3^-.
$$

对论文的 Step 1 系数，`c_{12}=E_1`、`c_{a2}=|t_2^{(1)}(t)|`、`c_{1b}=|t_1^{(1)}(t)|`、`c_{ab}=E_d^{(1)}(t)`，所以两条手性链都一般不消失。特别是在 `E_1=t_1=0` 的理想极限下，仍然同时保留 `J^+` 和 `J^-`，因此 Step 1 不是单一的 chiral `su(2)`；它最多只是 `so(4)` 内的一个对角 `su(2)` 候选，而不是可以直接写成单一单位四元数的块。


### 6.5 Step 2 的更强结论：没有固定块对角化

Step 2 的结果比“闭包涨到 `so(5)`”更强一点：它说明在固定的 `5\times 5` 实表示里，
不存在一个与整段 Step 2 哈密顿量同时对易的非平凡时间无关分块算符。

换句话说：

- 任何试图把 Step 2 一次性写成更小块的相似变换，都会被平凡 commutant 挡住；
- `X_6` 和 `X_{10}` 一旦进入，就会把 `X_2` 也拉出来，最后把五个 Majorana 全部接通；
- 所以 Step 2 的“简化”只能发生在时间方向，而不是空间方向。

对实际计算来说，这意味着 Step 2 最合理的处理顺序是：

1. 先选一个局部参考项，把最强的对易部分放进 interaction picture；
2. 再在短时间段上做分段 Magnus 或直接数值传播；
3. 不再期待一个全局的单位四元数或单一 `su(2)` 闭式。

如果一定要找一个“最像可积子问题”的切口，那也只能是把某一小段内近似常数的驱动先固定住，再把剩余部分当作扰动；这属于近似策略，不是 Step 2 的严格代数约化。
### 6.6 Interaction picture 数值试验结果

对 Step 2 做了 interaction picture 数值试验（脚本见 `step2_ip_solver.py`）。取
`H0 = E1 X1` 作为静态参考项，剩余部分在旋转框架中传播。

结果：

- **IP 重构正确性**：`U(t) = exp(A0 t) * U_I(t)` 与直接数值传播的偏差在
  $10^{-10}\sim 10^{-12}$ 量级，即数值精度级别。
- **Magnus(1+2) 在 IP 上的表现**：即使剥离了快速旋转，Magnus 误差仍保持
  $\sim 2.8\times 10^{-3}$ 量级，**与 E1 的大小几乎无关**（E1 从 0.01 扫到 2.0，
  误差变化不到 10%）。
- **`||A_I(t)||_F` 在整段 Step 2 中基本持平**，没有出现让 Magnus 收敛得更快的
  "小参数"窗口。

这说明 interaction picture 虽然把恒稳对易项剥离了，但**剩余代数在旋转框架下仍是满
的 so(5)**——`X5` 和 `X7` 的旋转会产生 `X4` 和 `X8`，继续接通全部 10 个生成元。所
以 interaction picture **没有真正降维**，它只是换了坐标系。

### 6.7 Step 2 处理方法的最终排序

结合以上所有测试，Step 2 各处理方法按优劣排序如下：

| 方法 | 严格性 | 可操作性 | 推荐度 |
|---|---|---|---|
| 数值 T exp（精确传播） | 精确 | 直接 | ★★★ |
| 分段 Magnus（短窗） | 可控误差 | 需要分窗调参 | ★★ |
| interaction picture + Magnus | 与直接 Magnus 几乎无异 | 多一步 IP 变换 | ★ |
| 单一单位四元数闭式 | **不成立** | — | ✗ |
| 全局 block 对角化 | **不存在** | — | ✗ |

简言之：**Step 2 最务实的就是保留完整 `Sp(2)` 时间有序指数，或做数值传播；代数上
没有捷径。**

---

## 7. Sp(2) 四元数矩阵表示下能否给出解析解？

这是把前三节结论收束之后的自然追问。回答要区分两层：**"解析形式"能做到什么程度**，
和**"闭式初等函数解"能否存在**。

### 7.1 先说结论

在 `Sp(2)` 四元数矩阵表示下：

- **可以**把时间有序指数 $\mathcal T\exp\big(\int K(t)dt\big)$ 转化为一组**精确的
  标量 ODE**——这是 Wei–Norman 分解或四元数 Riccati 方程，是严格的等价改写。
- **不能**对这组 ODE 给出用初等函数表达的闭式解——因为一般时变 `sp(2)` 哈密顿量不
  可积，这和 `SU(2)` 里"时变轴 ≠ 固定轴则无初等闭式"是同一类现象。

换句话说：Sp(2) 能给出**参数化解析形式**，但参数本身仍要数值积分；不能给出
**初等函数闭式**。

### 7.2 方法一：Wei–Norman 分解（10 参数）

Wei–Norman 定理说：对任何有限维李代数 $\mathfrak g$，时间有序指数可写成

$$
U(t)=\prod_{k=1}^{\dim\mathfrak g} e^{\,g_k(t)B_k},
$$

其中 $\{B_k\}$ 是 $\mathfrak g$ 的一组有序基，$g_k(t)$ 满足一组耦合 ODE。

对 `sp(2)`（$\dim=10$），取基底顺序

$$
L_1,L_2,L_3,\; Q_0,Q_1,Q_2,Q_3,\; R_1,R_2,R_3,
$$

其中 `L_a, R_a` 是左/右纯虚四元数块（各 3 个），`Q_0, Q_a` 是 off-diagonal 四元数
块（4 个）。则

$$
U(t)=e^{\ell_1 L_1}e^{\ell_2 L_2}e^{\ell_3 L_3}\;
      e^{q_0 Q_0}e^{q_1 Q_1}e^{q_2 Q_2}e^{q_3 Q_3}\;
      e^{r_1 R_1}e^{r_2 R_2}e^{r_3 R_3}.
$$

代入 $\dot U = K(t)U$ 得到 10 个实标量 ODE。

**优点**：精确，且参数有清晰的李群意义（左 `su(2)` 角、混合角、右 `su(2)` 角）。
**缺点**：ODE 耦合非线，对我们论文的正弦门控不做初等可积。

### 7.3 方法二：四元数 Riccati 方程（4 分量）

在 `Sp(2)` 的 big cell 上做极化分解：

$$
U = \begin{pmatrix} X & Y \\ Z & W \end{pmatrix},\qquad
q := Y X^{-1}\in\mathbb H.
$$

由分块运动方程导出

$$
\boxed{\dot q = B(t) + A(t)q - qC(t) - q B^\dagger(t) q},
$$

其中 $A(t),C(t)$ 是纯虚四元数（对角块），$B(t)$ 是一般四元数（off-diagonal 块）。

这就是 **quaternion Riccati 方程**：4 个实分量，非线性（含 $|q|^2 q$ 项）。

**优点**：维度最低（10 → 4），且 $q$ 直接对应波函数振幅比。
**缺点**：分母零点（big cell 边界）需坐标切换；仍然非初等可积。

### 7.4 为什么都不能初等闭式

这一切的根源不是方法选得不好，而是：

> **时变 `sp(2)` 哈密顿量 ≠ 可积系统。**

只有在以下情况之一成立时才有初等闭式：
- 整段 $K(t)$ 始终对易（即 $[K(t_1),K(t_2)]=0,\;\forall t_1,t_2$）；
- 整段 $K(t)$ 落在某个 `su(2)` 子代数里且方向固定；
- 门控 profile 恰好使 Wei–Norman ODE 分离（极罕见）。

对我们论文的三段门控：
- Step 1/Step 2 不满足任何一条——驱动方向随时间变化，且闭包涨满 `so(5)` 或 `so(4)`；
- Step 3 满足第二条——剥离 `u(1)` 后落入单一 `su(2)`，所以能写初等四元数。

### 7.5 实际可操作版本

因此，"Sp(2) 解析解"在实际项目中最有用的形态是：

| 形式 | 表达式 | 适用 |
|---|---|---|
| Step 3 单位四元数闭式 | $e^{\Phi X_{12}}\,(\cos\frac{\Theta}{2}+\hat n\sin\frac{\Theta}{2})$ | Step 3 ✓ |
| Step 3 Wei–Norman / Euler | 3 标量 ODE（已实现） | Step 3 ✓ |
| Step 1+2 Sp(2) 时间有序指数 | $\mathcal T\exp\big(\int K^{(1,2)}(t)dt\big)$ | Step 1/2 ✓ |
| Step 1+2 数值传播 | `solve_ivp` / `expm` 分段 | Step 1/2 ✓ |
| 全协议 Sp(2) Riccati | $\dot q = B+Aq-qC-qB^\dagger q$（4 ODE） | 全程可写但不初等 |

### 7.6 总结一句话

**Sp(2) 四元数表示是精确的、紧凑的、可以做 Wei–Norman 或 Riccati 参数化，但不能
把论文的三段门控压成一个初等函数闭式——除非只讨论 Step 3。**