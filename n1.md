# 从 $SO(5)$ 演化到理想编织：子空间判据、混合项与约束曲线

本文只比较 Majorana 算符的演化，不用波函数保真度。量子点的最终内部旋转被视为不可观测的自由度；因此只要求 $(\gamma_1,\gamma_2,\gamma_3)$ 的有效子空间正确复原并实现理想编织。

## 1. 从有效哈密顿量到 $SO(5)$

按顺序记

$$
\boldsymbol\gamma=(\gamma _1,\gamma _2,\gamma _3,\gamma _a,\gamma _b)^T .
$$

论文的有效模型为

$$
H(t)=iE_d\gamma_a\gamma_b+iE_1\gamma_1\gamma_2
 +it_2(t)\gamma_a\gamma_2-it_1(t)\gamma_b\gamma_1
 -it_3(t)\gamma_a\gamma_3 .
$$

写成

$$
H(t)=i\sum_{i<j}h_{ij}(t)\gamma_i\gamma_j,
$$

则

$$
h_{12}=E_1,\quad h_{42}=t_2,\quad h_{51}=-t_1,
\quad h_{43}=-t_3,\quad h_{45}=E_d .
$$

Heisenberg 方程给出

$$
\dot{\boldsymbol\gamma}=\Omega(t)\boldsymbol\gamma,
\qquad \Omega_{ij}=2h_{ij},
\qquad \Omega^T=-\Omega .
$$

因此存在唯一

$$
R(t)\in SO(5),\qquad \dot R=\Omega R,\quad R(0)=I_5,
$$

使得 $\boldsymbol\gamma(t)=R(t)\boldsymbol\gamma(0)$。这一步是精确的；困难只在于一般时变 $\Omega(t)$ 的时间序列指数通常没有初等闭式。

## 2. 理想编织是 $SO(5)$ 中的一个陪集

令

$$
P=\operatorname{diag}(1,1,1,0,0),\qquad P_\perp=I-P .
$$

前三个 Majorana 子空间记为 $\mathcal M=P\mathbb R^5$，量子点子空间记为 $\mathcal Q=P_\perp\mathbb R^5$。一个三段协议（一次交换）的算符变换是

$$
\gamma_1\mapsto\gamma_1,\qquad
\gamma_2\mapsto\gamma_3,\qquad
\gamma_3\mapsto-\gamma_2,
$$

故

$$
B_{23}=\begin{pmatrix}1&0&0\\0&0&-1\\0&1&0\end{pmatrix}\in SO(3).
$$

若总时长是论文图中连续两次交换的 $6\tau$，目标矩阵应改为

$$
B_{\rm target}=B_{23}^2=\operatorname{diag}(1,-1,-1).
$$

以下统一记 $B_{\rm target}$：一次交换取 $B_{23}$，两次交换取 $B_{23}^2$。

不观察量子点内部时，允许任意 $A_Q\in SO(2)$。理想结果的集合是

$$
\mathcal C_{\rm br}=
\left\{R_{\rm id}(A_Q)=
\begin{pmatrix}B_{\rm target}&0\\0&A_Q\end{pmatrix}:A_Q\in SO(2)\right\}.
$$

所以精确编织的必要充分条件为

$$
\boxed{P_\perp R(T)P=0},\qquad
\boxed{PR(T)P=B_{\rm target}} .
$$

第一式是没有从 $\mathcal M$ 泄漏到量子点，第二式是三 Majorana 内部正好实现辫子。由正交性，第一式同时保证 $PRP_\perp=0$，于是 $R(T)$ 自动为上述块对角形式。

可用下列量定义偏离：

$$
\varepsilon_{\rm leak}=\|P_\perp RP\|_F,
\qquad
\varepsilon_{\rm br}=\|PRP-B_{\rm target}\|_F/\sqrt6 .
$$

若量子点的块也想比较，则到陪集的距离为

$$
\varepsilon_{\rm coset}=\min_{A_Q\in SO(2)}
\left\|R-\operatorname{diag}(B_{\rm target},A_Q)\right\|_F .
$$

其中最优 $A_Q$ 是 $R_{QQ}$ 的正交极分解的旋转部分，因此这个最小化不引入新的动力学假设。

## 3. 李代数分解与局部精确条件

定义 $X_{ij}=i\gamma_i\gamma_j$。保持两个子空间的子代数为

$$
\mathfrak h=\mathfrak{so}(3)_{123}\oplus\mathfrak{so}(2)_{ab}
 =\operatorname{span}\{X_{12},X_{13},X_{23},X_{45}\} .
$$

其余六个生成元

$$
\mathfrak m=\operatorname{span}\{X_{1a},X_{1b},X_{2a},X_{2b},X_{3a},X_{3b}\}
$$

是混合方向，满足

$$
\mathfrak{so}(5)=\mathfrak h\oplus\mathfrak m .
$$

若 $R(T)$ 足够接近理想陪集，定义

$$
R(T)=R_{\rm id}(A_Q)e^{\delta},\qquad \delta=\log R_{\rm id}^{-1}R\in\mathfrak{so}(5).
$$

局部精确条件就是

$$
\delta_{\mathfrak m}=0,
\qquad
\delta_{\mathfrak{so}(3)}=0
\quad(\text{模 }2\pi\text{ 的旋转周期}).
$$

这给出六个泄漏约束和三个三 Majorana 内部约束。只有两个参数 $(E_1,t_1)$ 时，通用协议的精确解通常是孤立点；连续约束曲线需要额外对称性使多个约束恒等相关。

## 4. $(E_1,t_1)$ 的非可加性

写

$$
\Omega=\Omega_0+E_1\Omega_E+t_1\Omega_t(t),
$$

其中 $(\Omega_E)_{12}=2$，

$(\Omega_t)_{15}=-2g_1(t)$，$g_1(t)$ 是 $G_1$ 包络。令 $R_0$ 为 $E_1=t_1=0$ 的演化，定义

$$
A(s)=R_0^{-1}(s)\Omega_E R_0(s),\qquad
B(s)=R_0^{-1}(s)\Omega_t(s)R_0(s).
$$

则

$$
R(T)=R_0(T)\,\mathcal T\exp\int_0^T(E_1A+t_1B)ds .
$$

二阶展开为

$$
R=R_0\left[I+E_1K_E+t_1K_t+E_1^2K_{EE}+t_1^2K_{tt}+E_1t_1K_{Et}+O(3)\right],
$$

$$
K_{Et}=\int_{s_1>s_2}[A(s_1)B(s_2)+B(s_1)A(s_2)]ds_1ds_2 .
$$

因此

$$
\boxed{R(E_1,t_1)-R(E_1,0)-R(0,t_1)+R_0
 =E_1t_1R_0K_{Et}+O(3)} .
$$

非可加性的局部代数来源是

$$
\boxed{[X_{12},X_{15}]=-2iX_{25}},
\qquad X_{25}=i\gamma_2\gamma_b .
$$

所以联合项首先产生 $\gamma_2$-量子点的混合方向，再经基准演化共轭到其它方向。

## 5. 约束曲线的统一写法

设误差坐标为

$$
\mathcal E(E,t)=\big(\operatorname{vec}(P_\perp RP),\operatorname{vec}(PRP-B_{\rm target})\big).
$$

在 $(0,0)$ 附近

$$
\mathcal E=J_EE+J_tt+J_{EE}E^2+J_{Et}Et+J_{tt}t^2+O(3).
$$

若 $(J_E,J_t)$ 不共线，则局部只有 $E=t=0$。若 $J_t=\lambda J_E$，一阶约束曲线为

$$
\boxed{t=-E/\lambda} .
$$

令 $t(E)=\alpha E+\beta E^2+\cdots$，其中 $\alpha=-1/\lambda$，二阶系数需满足

$$
J_t\beta+J_{EE}+\alpha J_{Et}+\alpha^2J_{tt}=0 .
$$

若这个向量方程的各分量给出不同 $\beta$，曲线在二阶即不存在。混合有限差分

$$
\Delta_{\rm mix}R=R(E,t)-R(E,0)-R(0,t)+R_0
$$

是数值检验 $E_1t_1$ 项最直接的群矩阵方法。

## 6. 能产生约束曲线或消除低阶项的额外对称性

下面列出在有限维 $SO(5)$ 控制问题中可能出现的标准机制。它们是“曲线存在”的来源；没有其中任何一种时，不能预期有非平凡曲线。

### 6.1 保持子空间的李子群对称

若整个有效生成元（或经过完整协议的平均生成元）始终属于

$$
\mathfrak h=\mathfrak{so}(3)_{123}\oplus\mathfrak{so}(2)_{ab},
$$

则泄漏约束恒等成立，只剩 $SO(3)$ 内的角度约束。此时单个标量方程即可产生一条曲线，例如

$$
\theta(E,t)=\frac\pi2\pmod{2\pi}.
$$

当前三段协议的 $X_{42},X_{43},X_{15}$ 属于混合方向，因此该对称性不是自动满足的，必须靠脉冲抵消或暗态结构实现。

### 6.2 交换子为零的阿贝尔对称

若有效的误差方向 $A(s),B(s)$ 在所有时刻对易，则

$$
\mathcal T e^{\int(EA+tB)}=e^{E\int A+t\int B} .
$$

混合 Magnus 项严格为零，误差只依赖一个或少数几个累计角度。若只剩一个目标角度条件，就得到显式直线

$$
a_EE+a_tt=2\pi n
$$

（或椭圆等价式，取决于使用角度还是范数）。这是最强的可积情形。

### 6.3 参数符号的共轭反演

若存在 $S\in SO(5)$ 满足

$$
S\Omega_E S^{-1}=-\Omega_E,
\qquad S\Omega_0S^{-1}=\Omega_0,
$$

则

$$
R(-E,t)=SR(E,t)S^{-1}.
$$

所有误差坐标按 $S$ 的表示分成偶、奇两类。若目标 $R_{\rm id}$ 与 $S$ 对易，奇坐标含奇次 $E$，偶坐标含偶次 $E$。这会消去某些一阶或二阶系数，但单独的符号对称通常只给出 $E=0$ 轴，不保证一条补偿曲线。

对 $t$ 亦有完全相同的条件。

### 6.4 时间反演（palindromic）协议

若后半段是前半段的时间反演，并满足

$$
\Omega(T-s)=\Omega(s)
$$

（或先用固定 $S$ 共轭后满足该式），时间对称的 Magnus 展开会消去一类偶阶项，特别是标准对称分段下的二阶交换子项。若采用带符号反演的回波，

$$
\Omega(T-s)=S\Omega(s)S^{-1},
\quad S\Omega_{E,t}S^{-1}=-\Omega_{E,t},
$$

则 $(E_1,t_1)$ 的一阶动态项被抵消；再施加递归对称可逐阶消去。

“哪一奇偶阶消失”取决于时间反演中是否带负号，但判据始终是把后半段生成元写成前半段的共轭或逆元。

### 6.5 群回波（conjugation echo）

若可实现 $Q_E$ 使

$$
Q_E X_{12}Q_E^{-1}=-X_{12},
\qquad Q_E X_{23}Q_E^{-1}=X_{23},
$$

则拼接一对共轭段可抵消 $E_1$ 的一阶动态相位。对 $t_1$ 要求

$$
Q_tX_{15}Q_t^{-1}=-X_{15}.
$$

若 $Q_E,Q_t$ 同时反演两个误差方向，则 $E_1t_1$ 交换子也可被反演；四段或八段的群平均可把二阶项压到更高阶。没有可实现的 $Q$，这只是形式上的对称，不是当前硬件的约束曲线。

### 6.6 参数交换对称与固定线

若存在 $S$ 使完整协议满足

$$
S\Omega(E,t;s)S^{-1}=\Omega(t,E;s),
\qquad SR_{\rm id}S^{-1}=R_{\rm id},
$$

则误差满足 $\mathcal E(E,t)=S\mathcal E(t,E)S^{-1}$。在交换对称的误差分量中，固定线

$$
\boxed{t=E}

$$

自动消去反对称分量；若带一个符号反演，则固定线变为 $t=-E$。这通常只减少约束数，剩余对称误差仍需另外为零。

### 6.7 暗态或平行输运对称

若存在随时间变化的三维投影 $P(t)$，满足

$$
[H(t),P(t)]-i\dot P(t)=0,
$$

则该子空间严格平行输运，不发生泄漏。若其 Wilczek--Zee holonomy 在一周期为 $B_{\rm target}$，就得到精确编织，即使瞬时哈密顿量含混合生成元。这个条件是几何的，通常比 $E_1=t_1=0$ 更宽；它要求设计额外控制使 $P(t)$ 的连接满足目标 holonomy。

### 6.8 交换子补偿与新增生成元

因为

$$
[X_{12},X_{15}]=-2iX_{25},
$$

若能独立控制 $X_{25}$，可设置补偿脉冲使

$$
\Omega_{25}^{\rm ctrl}=-\Omega_{25}^{(2)}(E_1t_1)+O(3).
$$

这样可消除二阶混合误差。继续加入由李括号产生的方向，可做递归补偿。若控制生成元的 Lie 闭包为整个 $\mathfrak{so}(5)$，则系统在群上可控；精确实现编织等价于寻找控制参数使终点落在 $\mathcal C_{\rm br}$。这保证可达性，不保证原来两参数平面中存在曲线。

### 6.9 $SO(5)$ 的周期与 $Spin(5)$ 中心

在 $SO(5)$ 算符变换层面，旋转角相差 $2\pi$ 给出同一矩阵；因此动态误差若满足

$$
\Phi(E,t)=2\pi n
$$

也可能回到同一 $SO(5)$ 结果。若要求旋量或波函数的完整幺正算符，则 $Spin(5)\to SO(5)$ 的双覆盖使 $U$ 与 $-U$ 需要区分，周期通常变为 $4\pi$。这是闭合的周期族，不是低阶抵消产生的局部曲线。

### 6.10 奇偶、手征和反线性对称

总费米子奇偶与所有双线性 $X_{ij}$ 对易，因此只把 Hilbert 空间分成奇偶块，不会单独消除 $SO(5)$ 的混合矩阵元。若另有一个反射或反线性对称 $\Theta$，满足

$$
\Theta\Omega(t)\Theta^{-1}=\pm\Omega(T-t),
$$

它可以把某些矩阵元限制为零，但必须检查该对称是否保留 $B_{\rm target}$。仅有总奇偶守恒不能产生 $(E_1,t_1)$ 的补偿曲线。

## 7. “不同阶约束曲线”的实际判定

对给定协议，按下列顺序检查即可：

1. 计算 $(J_E,J_t)$。若不共线，原点附近没有非平凡一阶曲线。
2. 若共线，求 $t=\alpha E+\beta E^2+\cdots$，检查二阶向量方程；其中 $E t$ 项由 $K_{Et}$ 给出。
3. 用时间反演、符号回波或交换对称判断某些 $J$ 或 $K$ 是否恒为零；若二阶混合项消失，再检查三阶。
4. 若误差坐标在所有阶都被限制到一个标量角度 $\Phi(E,t)$，精确曲线为

$$
\Phi(E,t)=2\pi n
$$

（编织角本身则取 $\Phi=\pi/2+2\pi n$，按单次或双次交换调整归一化）。
5. 若仍有两个以上独立误差方向，则解集通常是离散点；需加入额外可控生成元或增加参数维数。

因此，“所有额外对称性”不能替代具体协议的计算：对称性先决定哪些阶数、哪些李代数方向必为零；剩余系数仍由 $t_2(t),t_3(t),E_d(t)$ 和时间 $\tau$ 决定。对当前模型最有希望的三种机制是：$E_1$ 的符号回波、关于三段协议的时间镜像、以及直接加入 $X_{25}=i\gamma_2\gamma_b$ 的二阶交换子补偿。

## 8. 按阶数整理的对称性与约束

这里的“$n$ 阶约束”指误差坐标的 Taylor/Magnus 系数从 $n$ 阶开始才可能出现；它不是说最终解一定是一条 $n$ 阶多项式曲线。

| 机制 | 代数条件 | 被消去的项 | 可能的局部约束 |
|---|---|---|---|
| 子群保持 | $\Omega(s)\in\mathfrak h$ | 全部泄漏方向，精确到所有阶 | 一个 $SO(3)$ 角度方程，通常是一条曲线 |
| 阿贝尔误差 | $[A(s_1),B(s_2)]=0$ | 所有交换子，含 $Et$ 项 | $a_E E+a_t t=2\pi n$ |
| 单参数符号反演 | $S\Omega_E S^{-1}=-\Omega_E$ | 相应误差坐标的偶/奇混合 | 常给 $E=0$ 轴；需另一条件才成曲线 |
| 双参数符号反演 | 同时反演 $E,t$ | $E,t$ 的一阶项按表示分块 | 可能出现 $t=\pm E+O(E^2)$ |
| 时间镜像 | $\Omega(T-s)=S\Omega(s)S^{-1}$ | 取决于 $S$ 的 Magnus 奇偶阶 | 若只剩一标量角度，可得二阶或更高阶曲线 |
| 群回波 | $QXQ^{-1}=-X$ | 指定误差的一阶动态项 | 误差从 $O(E)$ 提升到 $O(E^2)$ |
| 双回波/群平均 | 误差表示的群平均为零 | 一阶、二阶甚至指定的 $n$ 阶项 | $t(E)=\alpha E+O(E^{n+1})$ |
| 参数交换 | $S\Omega(E,t)S^{-1}=\Omega(t,E)$ | 反对称误差分量在 $t=E$ 上为零 | 固定线 $t=E$ 或 $t=-E$ |
| 暗态平行输运 | $[H,P]-i\dot P=0$ | 泄漏项精确为零 | holonomy 角度方程给出曲线 |
| 交换子补偿 | 加入 $X_{25}$ 并令其脉冲抵消 $Et$ | 指定二阶混合项 | 可把一阶补偿曲线延伸到二阶 |
| $SO(5)$ 周期闭合 | 误差角 $\Phi=2\pi n$ | 不是 Taylor 项，而是全局周期 | 闭合曲线或离散周期点 |

更严格地说，令 $F(E,t)=\mathcal E(E,t)$ 为所有误差坐标组成的向量，

$$
Z=F^{-1}(0).
$$

若在某个解点 $p$ 处 Jacobian 的秩为 $r$，则隐函数定理给出

$$
\dim Z=2-r
$$

（这里参数空间只有 $E,t$ 两维）。因此：

* $r=2$：局部是孤立点；
* $r=1$：局部是一条约束曲线；
* $r=0$：误差在该点附近恒为零，形成二维解域。

对称性的作用就是强制若干误差坐标恒等为零，或使 Jacobian 的列向量相关，从而把 $r=2$ 降为 $r=1$。

若存在一条解析曲线 $t=t(E)$，把它代入

$$
F(E,t(E))=0,
\qquad t(E)=\alpha E+\beta E^2+\chi E^3+\cdots
$$

即可逐阶得到约束：

$$
J_t\alpha+J_E=0,
$$

$$
J_t\beta+J_{EE}+\alpha J_{Et}+\alpha^2J_{tt}=0,
$$

三阶还要加入 $J_{EEE},J_{EEt},J_{Ett},J_{ttt}$ 与 $\chi$。如果某一阶的向量方程不能由同一个标量系数满足，曲线就在该阶终止。这是判定“候选曲线”是否真实存在的严格方法。

## 9. 对当前协议的可实现性判断

当前哈密顿量显式含有混合生成元 $X_{42},X_{43},X_{15}$。所以子群保持不是瞬时成立的；它只能通过完整路径的回波、暗态或精确闭合实现。$E_1$ 的符号反演可由局域门控或势垒调节实现时，最直接地消去 $X_{12}$ 的一阶贡献。$t_1$ 与 $t_2$ 由同一个 $G_1$ 控制时，不能把 $t_1$ 当成完全独立的第二个旋钮，参数空间实际上是由样品关系 $t_1=t_1(E_1)$ 预先选定的一条路径。

若 $(E_1,t_1)$ 都固定且非零，而没有额外回波或补偿生成元，则六个泄漏坐标和三个内部坐标一般独立，$r=2$ 的结论只说明在二维参数平面中解通常是离散的；不能仅凭 $SO(5)$ 闭包推出一条普适曲线。若加入可控 $X_{25}=i\gamma_2\gamma_b$ 或其它独立生成元，才有足够自由度逐阶消去 $Et$ 及更高阶交换子。
