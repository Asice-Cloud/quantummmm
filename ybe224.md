# Jordan–Wigner 映射与两比特到 Kitaev 链的嵌入

本文档给出完整的 Jordan–Wigner (JW) 映射推导要点，并把前面两比特 Pauli 张量基的模型明确嵌入到费米子 BdG（Kitaev）表示中。目标是把 `ybe223.md` 中的两比特有效模型

- 在自旋基下写成 Pauli 张量项（XX, YY, XY, YX, ZI, IZ 等），
- 通过 JW 映射转为费米子二次项（hopping, pairing, chemical potential），

并给出显式的两站点 BdG 矩阵和一般化到长度 L 的 Kitaev 链的构造方法。

## 1 基本约定与 JW 映射

站点编号按 $j=1,2,\dots$。我们使用单比特 Pauli 矩阵 $\sigma_j^{x,y,z}$ 与升降算符
$$
\sigma_j^{\pm} = \tfrac{1}{2}(\sigma_j^x \pm i\sigma_j^y).
$$
Jordan–Wigner 映射定义费米子算符 $c_j, c_j^\dagger$ 为
$$
c_j = \Bigl(\prod_{k<j}\sigma_k^z\Bigr)\sigma_j^-,\qquad
c_j^\dagger = \Bigl(\prod_{k<j}\sigma_k^z\Bigr)\sigma_j^+.
$$
这保证了 $\{c_i,c_j^\dagger\}=\delta_{ij}$ 与反对易关系。

常用等价关系（便于替换）：

- 单站点：$\sigma_j^z = 2n_j - 1$, 其中 $n_j = c_j^\dagger c_j$。
- 邻位组合：
	- $\sigma_j^x\sigma_{j+1}^x + \sigma_j^y\sigma_{j+1}^y = 2\,(c_j^\dagger c_{j+1} + c_{j+1}^\dagger c_j)$ （hopping），
	- $\sigma_j^x\sigma_{j+1}^x - \sigma_j^y\sigma_{j+1}^y = 2\,(c_j^\dagger c_{j+1}^\dagger + c_{j+1} c_j)$ （pairing）。

当相距不为 1 时，JW 前缀字符串 $\prod_{k<j}\sigma_k^z$ 会留下长串算子；但对于紧邻站点，前缀常在相乘时抵消，从而得到二次费米子项。

## 2 两比特 (j=1,2) 的显式映射

取两站点自旋基的标准顺序 $|00\rangle,|01\rangle,|10\rangle,|11\rangle$。下面列出常见 Pauli 张量在 JW 映射下的结果（只保留二次费米子项与常数）：

- $X_1 X_2 + Y_1 Y_2 \longrightarrow\; 2\,(c_1^\dagger c_2 + c_2^\dagger c_1)$ （hopping），
- $X_1 X_2 - Y_1 Y_2 \longrightarrow\; 2\,(c_1^\dagger c_2^\dagger + c_2 c_1)$ （pairing），
- $X_1 Y_2 + Y_1 X_2 \longrightarrow\; 2i\,(c_1^\dagger c_2 - c_2^\dagger c_1)$ （带相位的 hopping），
- $X_1 Y_2 - Y_1 X_2 \longrightarrow\; 2i\,(c_1^\dagger c_2^\dagger - c_2 c_1)$ （带相位的 pairing），
- $Z_1 I_2 \longrightarrow\; 2n_1 - 1$,  $I_1 Z_2 \longrightarrow\; 2n_2 - 1$。

注：常数因子（如 2）可并入系数定义，符号与因子取决于 Pauli 的具体定义與归一约定；在数值实现中应按统一约定匹配系数。

## 3 从 `ybe223.md` 的模型到两站点 BdG

在 `ybe223.md` 中我们把有效两能级表示写成
$$
H_{\rm eff} = d_x\sigma_x + d_y\sigma_y + d_z\sigma_z.
$$
在子空间 $\{|01\rangle,|10\rangle\}$ 下，这对应到费米子 BdG 的自然嵌入。把自旋项按张量展开为
$$
H_{\rm spin} = a\,X_1 X_2 + b\,Y_1 Y_2 + c\,X_1 Y_2 + c'\,Y_1 X_2 + \mu_1 Z_1 + \mu_2 Z_2 + \cdots
$$
利用第 2 节映射，得到费米子二次 Hamiltonian（忽略整体常数）：
$$
H_f = -\sum_{j=1}^2 \tilde\mu_j\,n_j - t\,(c_1^\dagger c_2 + c_2^\dagger c_1) + \Delta\,(c_1 c_2 + c_2^\dagger c_1^\dagger) + i\Phi\,(c_1^\dagger c_2 - c_2^\dagger c_1) + i\Xi\,(c_1^\dagger c_2^\dagger - c_2 c_1).
$$

系数线性关系示例（把常数因子吸收进定义）：

- hopping: $t = (a+b)/2$ （来自 $X X + Y Y$），
- pairing: $\Delta = (a-b)/2$ （来自 $X X - Y Y$ 或反对称组合），
- 相位跳跃/有向性: $\Phi,\Xi$ 由 $X Y, Y X$ 的不对称组合决定，控制 hopping/pairing 的相位，
- 化学势: $\tilde\mu_{1,2}$ 由 $\mu_{1,2}$（Z 项）线性给出。

把 `ybe223.md` 的典型示例
$$
H_{\rm eff}(u)=\begin{pmatrix}\delta & e^{-iu}\\ e^{iu} & -\delta\end{pmatrix}
$$
嵌入后，可取如下对应（单位与因子按约定）：

- $t(u)=\mathrm{Re}\,e^{iu}=\cos u$，
- $\Delta(u)= -\mathrm{Im}\,e^{iu}= -\sin u$，
- 对角差由 $Z$ 项产生（$\propto\delta$）。

因此一个实用的两站点 BdG 写法为
$$
H_f(u) = -\delta\,(n_1 - n_2) - t(u)\,(c_1^\dagger c_2 + c_2^\dagger c_1) + \Delta(u)\,(c_1 c_2 + c_2^\dagger c_1^\dagger).
$$

写成 BdG 矩阵（Nambu 基 $\Psi= (c_1,c_2,c_1^\dagger,c_2^\dagger)^T$）：
$$
H_{\rm BdG} = \frac{1}{2}\Psi^\dagger \begin{pmatrix}
 -\tilde{\mu}_1 & -t & 0 & \Delta \\
 -t & -\tilde{\mu}_2 & -\Delta & 0 \\
 0 & -\Delta^* & \tilde{\mu}_1 & t \\
 \Delta^* & 0 & t & \tilde{\mu}_2
\end{pmatrix} \Psi.
$$

本征能谱可以写出解析式（两站点简单情形）：
$$
E = \pm\sqrt{t(u)^2 + \Delta(u)^2 + \delta^2} = \pm\sqrt{1+\delta^2}\quad(\text{若 }t^2+\Delta^2=1).
$$

## 4 从两站点到长度 L 的 Kitaev 链

把两站点模型嵌入更长链的两种常见方式：

1. 把两站点作为链的前两站（j=1,2），并在 j=2..L-1 之间使用标准 Kitaev 链耦合（常数 $t_0,\Delta_0,\mu_0$），保持 j=1,2 上由 R(u) 控制的局部耦合；
2. 把两站点嵌入为链中间的一段局部操控片段以测试局域化与散射对零模的影响。

标准 Kitaev 链（最近邻） BdG:
$$
H_{\rm Kitaev} = -\sum_{j=1}^{L-1}\bigl(t_0 c_j^\dagger c_{j+1} + \Delta_0 c_j c_{j+1} + {\rm h.c.}\bigr) - \mu_0\sum_{j=1}^L n_j.
$$

把两站点的局域 $t(u),\Delta(u),\tilde\mu$ 加到链的前两项上，构造完整 BdG 矩阵并对角化可以检测：

- 是否出现零能本征值（MZM）以及其波函数是否在链两端指数局域化，
- 能量随链长 L 的退化行为（MZM 分裂随 L 指数小），
- 对局域扰动的稳定性。

## 5 建议的数值脚本与输出

实现建议（我将按 TODO 实现）：

- 脚本 `tools/embed_kitaev.py`：从给定 Pauli 项系数或 `H(u)` 构造自旋 Hamiltonian（矩阵形式），用 JW 映射替换为 fermionic 二次项，构造 BdG 矩阵并对角化。
- 输出：本征能谱随参数 u 的变化图、端点态密度图、随 L 的能量分裂曲线。

