# PRB111 动力学相位抵消：KAK 分解、切空间分析、完整设计框架

> 目标：在 $E_1 \neq 0$（MZM 杂化）的前提下，设计三段 braiding 协议的控制波形，
> 使末态 $U(T)$ 精确等于理想编织 $U_{\text{ideal}}$。

---

## 一、问题陈述

### 1.1 系统

PRB111 有效模型（Zhang et al., 2025）：

$$H_{\text{EM}}(t) = iE_d\gamma_a\gamma_b + iE_1\gamma_1\gamma_2 + i|t_2|\gamma_a\gamma_2 - i|t_1|\gamma_b\gamma_1 - i|t_3|\gamma_a\gamma_3$$

- 5 个 Majorana：$\gamma_1,\gamma_2,\gamma_3$（MZM）+ $\gamma_a,\gamma_b$（ancilla）
- 李代数：$\mathfrak{so}(5) \cong \mathfrak{sp}(2)$，10 维，单李代数（不可分解）
- 设备约束：G1 控制 $t_1$，G2 控制 $t_2,t_3$（共享），$E_d$ 可能独立

### 1.2 三段协议

| Step | 时间 | 物理操作 | 控制波形 |
|------|------|---------|---------|
| 1 | $0 \to \tau_1$ | $\gamma_2$ 移入量子点 | $t_2 \uparrow, E_d \downarrow, t_3=0$ |
| 2 | $\tau_1 \to \tau_1+\tau_2$ | $\gamma_2 \to \gamma_3$ 切换 | $t_2 \downarrow, t_3 \uparrow$ |
| 3 | $\tau_1+\tau_2 \to T$ | $\gamma_3$ 退出，恢复 | $t_3 \downarrow, E_d \uparrow, t_2=0$ |

### 1.3 核心困难

$E_1 \neq 0$ 时，$iE_1\gamma_1\gamma_2$ 与编织生成元 $t_2,t_3$ 同在 $\mathfrak{k}$ 子空间内，
产生不可消除的动力学相位污染。**需通过控制波形设计，使总演化 $U(T)$ 的 Cartan 因子
精确等于理想值。**

---

## 二、李代数结构：$\mathfrak{sp}(2) = \mathfrak{k} \oplus \mathfrak{p}$

### 2.1 Cartan 分解

| 子空间 | 维数 | 矩阵形式 | 物理生成元 |
|--------|:---:|------|------|
| $\mathfrak{k}$ | 6 | $\begin{pmatrix}A&0\\0&D\end{pmatrix}$，$A^\dagger = -A$ | $E_1, t_2, t_3$ 及其对易闭包 |
| $\mathfrak{p}$ | 4 | $\begin{pmatrix}0&B\\-\bar B&0\end{pmatrix}$，$B^\top = B$ | $E_d, t_1$ 及其对易闭包 |

总维数 $6+4 = 10 = \dim\mathfrak{sp}(2)$。

### 2.2 切空间维度

演化算符 $U(T;\mathbf{p})$ 是参数向量 $\mathbf{p} \in \mathbb{R}^m$ 到李群 $Sp(2)$ 的映射。
在参数空间某点 $\mathbf{p}_0$ 的切空间为：

$$\mathcal{T}_{\mathbf{p}_0} = \operatorname{span}\left\{\frac{\partial U}{\partial p_i}\bigg|_{\mathbf{p}_0} \cdot U^\dagger\right\} \subset \mathfrak{sp}(2)$$

**关键约束**：$\dim\mathcal{T}_{\mathbf{p}_0} \leq \min(m, 10)$。当 $m \geq 10$ 且切映射是满射时，
隐函数定理保证局部存在 $\mathbf{p}$ 使 $U(T;\mathbf{p}) = U_{\text{ideal}}$。

---

## 三、KAK 分解：分离几何与动力学

### 3.1 分解定理

任意 $U \in Sp(2)$ 可唯一（模 Weyl 群作用）分解为：

$$\boxed{U = k_1 \cdot a \cdot k_2}$$

其中 $a = \operatorname{diag}(e^{i\theta_1}, e^{-i\theta_1}, e^{i\theta_2}, e^{-i\theta_2})$（极大环面 $\cong U(1)^2$），
$k_1,k_2 \in Sp(2)$。

### 3.2 物理含义

| 因子 | 编码内容 | 提取方法 |
|------|---------|---------|
| $a$ | **动力学相位**（$\theta_1,\theta_2$ = $U$ 的两个独立本征角） | $\operatorname{Tr}U,\;\operatorname{Tr}U^2$ |
| $k_1,k_2$ | **几何旋转**（MZM 子空间的基矢方向） | $U$ 的广义特征值分解 |

$E_1=0$ 的标准协议给出参考值 $U_{\text{ideal}} = k_1^{\text{id}} a^{\text{id}} k_2^{\text{id}}$。
$E_1 \neq 0$ 时，$a \neq a^{\text{id}}$——动力学相位被污染。

### 3.3 抵消条件（仅 3 个方程）

$$\boxed{\begin{aligned}
C_1(\mathbf{p}) &:= \theta_1(U(T;\mathbf{p})) - \theta_1^{\text{id}} = 0 \pmod{2\pi} \\[4pt]
C_2(\mathbf{p}) &:= \theta_2(U(T;\mathbf{p})) - \theta_2^{\text{id}} = 0 \pmod{2\pi} \\[4pt]
C_3(\mathbf{p}) &:= R_{11}[U(T;\mathbf{p})] - 1 = 0
\end{aligned}}$$

$C_3$ 等价于要求 $\gamma_1$ 分量的演化保持恒等——编织不污染 $\gamma_1$。

---

## 四、参数化分析与切空间秩（严格结果）

### 4.1 6 参数余弦协议：不可能

原始参数化：$\mathbf{p} = (\tau_1,\tau_2,\tau_3,\; t_1^{\text{amp}},\; t_c,\; E_0)$，每步共享 $t_c,E_0$。

**切空间秩**（数值严格验证，SVD）：

| 操作点 | $\dim\mathcal{T}$ | 结论 |
|--------|:---:|------|
| $E_1 = 0$（理想点） | **2 / 10** | 极其不足 |
| $E_1 = 0.01$（物理点） | **4 / 10** | 不足 |

奇异值谱（$E_1=0.01$）：$[3.34\times10^3,\; 2.39\times10^2,\; 2.65\times10^{-1},\; 4.31\times10^{-2},\; {\sim}10^{-3},\; {\sim}10^{-3}]$。

**结论**：6 参数余弦协议的切空间维数远小于 $\dim\mathfrak{sp}(2)=10$。
隐函数定理保证 **在此参数化内，$U(T;\mathbf{p}) = U_{\text{ideal}}$ 无局部解**。
之前所有优化搜索失败的根本原因——不是搜得不够细，是参数化维度不足。

### 4.2 12 参数逐步独立协议：完全可行

扩展参数化：每步独立 $t_c, E_d, t_1$：

$$\mathbf{p} = (\tau_1,\tau_2,\tau_3,\; t_{1}^{(1)},t_{1}^{(2)},t_{1}^{(3)},\; t_c^{(1)},t_c^{(2)},t_c^{(3)},\; E_d^{(1)},E_d^{(2)},E_d^{(3)})$$

**切空间秩**（$E_1=0.01$，$t_1^{(i)}$ 基线非零）：

| 指标 | 结果 |
|------|------|
| $\dim\mathcal{T}$ | **10 / 10** ✓ |
| 最小非零奇异值 | $2.31 \times 10^{-2}$ |

奇异值：$[2.37\times10^3,\; 8.41\times10^2,\; 6.94\times10^2,\; 2.38\times10^2,\; 5.52\times10^1,\;
5.31\times10^1,\; 1.10\times10^0,\; 1.32\times10^{-1},\; 9.67\times10^{-2},\; 2.31\times10^{-2}]$。

**结论**：切映射是满射。隐函数定理保证 **在 12 维参数空间中局部存在解 $\mathbf{p}^*$ 使 $U(T;\mathbf{p}^*) = U_{\text{ideal}}$**。

### 4.3 技术注：$|t_1|$ 的非可微性

物理上 $t_1$ 是耦合强度，取绝对值 $|t_1|$ 出现在 $B,C$ 块中。在 $t_1=0$ 处，
$|t_1|$ 的左右导数不等（$+1$ vs $-1$），中心差分 $\frac{|+\varepsilon|-|-\varepsilon|}{2\varepsilon}=0$，
导致切向量秩损失。**三个 $t_1$ 参数均需非零基线**——这在 $E_1 \neq 0$ 时物理上合理。

---

## 五、绝热框架：设计方程组的显式形式

绝热极限下，动力学相位和几何相位可独立计算。单次函数评估 $O(N)$，全程不需 ODE。

### 5.1 瞬时本征值（闭式代数函数）

$K(t)$（$H$ 在 $\mathfrak{k}$ 中的投影）的本征值为：

$$\lambda_{1,2}(t) = \sqrt{\frac{a_2(t) \pm \sqrt{\Delta(t)}}{2}}$$

其中：

$$a_2(t) = \frac{E_1^2 + E_d^2 + t_1^2 + t_2^2 + t_3^2}{2}$$

$$\Delta(t) = E_1^2E_d^2 + E_1^2t_3^2 - 2E_1E_d t_2|t_1| + t_1^2t_2^2 + t_1^2t_3^2$$

### 5.2 动力学相位

$$\boxed{\theta_i^{\text{dyn}} = \int_0^{T} \lambda_i(t)\,dt}$$

被积函数是控制参数 $\{t_1,t_2,t_3,E_d\}$ 的已知显式代数函数——直接数值积分。

### 5.3 几何相位（投影算符乘积）

$$P(t) = \frac{\lambda(t) I - H(t)}{2\lambda(t)},\quad \lambda = \max(\lambda_1,\lambda_2)$$

$$\boxed{U_{\text{geo}} = P(t_N) P(t_{N-1}) \cdots P(t_1)}$$

从 $U_{\text{geo}}$ 提取 $\gamma_1^{\text{geo}},\gamma_2^{\text{geo}}$ 和 $R_{11}$。

### 5.4 完整设计方程组

$$\boxed{\begin{aligned}
C_1(\mathbf{p}) &:= \left(\int_0^{T} \lambda_1(t)\,dt + \gamma_1^{\text{geo}}\right) \bmod 2\pi - \theta_1^{\text{id}} = 0 \\[8pt]
C_2(\mathbf{p}) &:= \left(\int_0^{T} \lambda_2(t)\,dt + \gamma_2^{\text{geo}}\right) \bmod 2\pi - \theta_2^{\text{id}} = 0 \\[8pt]
C_3(\mathbf{p}) &:= R_{11}[U_{\text{geo}}] - 1 = 0
\end{aligned}}$$

- 方程数：**3**（2 个 Cartan 角 + 1 个 $\gamma_1$ 约束）
- 参数数：**12**（逐步独立协议）
- 自由度：$12 - 3 = 9$，解空间充裕

---

## 六、计算流程（三阶段）

### Phase I：绝热框架求解（快速探索）

```
  参数 p → 生成波形 {t₁,t₂,t₃,Ed}_k
         → 计算 λ₁₂(tₖ) → 积分 → θᵈʸⁿ
         → 计算 P(tₖ) → 矩阵乘积 → U_geo → γᵍᵉᵒ, R₁₁
         → C₁,C₂,C₃ → 牛顿法/信赖域迭代
```

全程不需求解 ODE，无 stiffness 发散，单次评估 $O(N)$。

### Phase II：Sp(2) 传播验证（精确确认）

对 Phase I 找到的候选参数 $\mathbf{p}^*$，用 RK4 + Gram-Schmidt 正交化
传播完整的 $4\times 4$ Sp(2) 矩阵（NPS ≥ 3000），直接计算 $U(T)$ 与 $U_{\text{ideal}}$ 的偏离。

### Phase III：非绝热修正（如果必要）

若非绝热修正显著，用 Riccati ODE $O(E_1)$ 微扰理论（见 `14.md`）做一阶修正：

$$q^1 \text{ 满足线性 ODE: } \dot q^1 = C^1 + D^0 q^1 - q^1 A^0 - q^0 B^0 q^1 - q^1 B^0 q^0$$

---

## 七、与 PRB113 方案的对比

| | PRB113（瞬时简并） | 本方案（KAK 全局） |
|---|---|---|
| 核心思想 | 瞬时 $\lambda_1=\lambda_2$ → 动力学消失 | 全局 $a = a^{\text{id}}$ → 动力学匹配 |
| 条件类型 | 瞬时（需 $\Delta=0$ 在某个 $t$） | 全局（积分约束） |
| $E_1\neq0$ 可行性 | **不可能**（需 $t_3=0$，与编织矛盾） | **可能**（切空间满秩） |
| 额外自由度 | 多余 MZM 对 | 逐步独立控制参数 |
| 计算复杂度 | 瞬时条件（$O(1)$/步） | 数值积分 + 矩阵乘积（$O(N)$） |

PRB113 瞬时简并方案在 $E_1 \neq 0$ 时不可行（严格证明见 `0/rigorous_proof_degeneracy.md`）。
本方案的全局抵消策略是唯一可行的路径。

---

## 八、总结

1. **$\mathfrak{sp}(2)$ 是 10 维单李代数**——不可能通过李代数分解将 $E_1$ 隔离到可因子化的子代数
2. **KAK 分解将问题简化为 3 个约束方程**：2 个 Cartan 角 + 1 个 $\gamma_1$ 保真度
3. **6 参数余弦协议切空间秩 = 4/10**——严格不可能（解释了所有历史优化失败）
4. **12 参数逐步独立协议秩 = 10/10**——满秩，隐函数定理保证局部解存在
5. **绝热框架提供 $O(N)$ 评估**：代数本征值 + 数值积分 + 投影算符乘积，无需 ODE

下一步：在 12 维参数空间中用牛顿法/信赖域方法数值求解 $C_{1,2,3}(\mathbf{p}) = 0$。
