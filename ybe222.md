# Pauli 张量基、非阿贝尔演化与 MZM→ABS 的统一描述（含控制电路设计）



# 一、核心思想总结

> **Pauli 张量基的作用不是替代 Majorana 描述，而是提供一个“可控制的算符空间坐标”，从而：**
>
> * 将非阿贝尔性转化为“路径问题”
> * 将 MZM/ABS 区分转化为“几何 vs 动力学竞争”
> * 直接指导实验控制序列设计



# 二、基本结构链条

从演化算符出发：

$$
R(u)=\mathcal{T}\exp\Big(-i\int_0^u H(s)ds\Big)
$$

Hamiltonian 用 Pauli 张量基展开：

$$
H(u)=\sum_{\alpha,\beta} h_{\alpha\beta}(u),\sigma^\alpha\otimes\sigma^\beta
$$



## 层级结构

| 层级 | 内容                            |
| -- | -- |
| 1  | Pauli 系数 (h_{\alpha\beta}(u)) |
| 2  | Hamiltonian (H(u))            |
| 3  | 本征态与能谱                        |
| 4  | Berry connection              |
| 5  | 非阿贝尔演化                        |



# 三、非阿贝尔性的代数判据

非阿贝尔性的最直接判据：

$$
[H(u_1),H(u_2)] \neq 0
$$



## 重要意义

* 不需要计算本征态
* 不需要 Berry connection
* 直接从 Pauli 系数判断



# 四、从 R(u) 到有效 Hamiltonian（实例）

考虑一个简化的 four-level R 矩阵：

$$
R(u)=
\begin{pmatrix}
\cos u & 0 & 0 & i\delta \sin u \\
0 & \sin u & \cos u & 0 \\
0 & \cos u & \sin u & 0 \\
i\delta \sin u & 0 & 0 & \cos u
\end{pmatrix}
$$



## 定义 Hamiltonian

$$
H(u)= i\partial_u R(u)R^{-1}(u)
$$



## 得到 Pauli 表达（主导项）

$$
H(u)\approx
\cos uXX + \sin uYY + \delta ZZ
$$

其中：

$u:0→2π$（一圈闭路径）

$XX=\sigma_x \otimes \sigma_x, YY,ZZ同理$	​




# 五、低能子空间投影

选取子空间：

$$
\mathcal S = {|01\rangle,\ |10\rangle}
$$



## 有效 Hamiltonian

$$
H_{\text{eff}}(u)=
\begin{pmatrix}
\delta & e^{-iu}\\
e^{iu} & -\delta
\end{pmatrix}
$$



## Bloch 向量形式

$$
H_{\text{eff}}(u)=\vec d(u)\cdot \vec \sigma
$$

$$
\boxed{
\vec d(u) = (\cos u,\ \sin u,\ \delta)
}
$$



# 六、MZM 与 ABS 的统一描述



## 情况 1：MZM（理想）

$$
\delta = 0
$$
此时
$$
H(u)=\cos u XX+\sin u YY
$$
本征态
$$
|\pm(u)\rangle = \frac{1}{sqrt(2)} \begin{pmatrix}
-e^{-iu/2}\\
\pm e^{iu/2}
\end{pmatrix}
$$

Berry Connection:
$$
A(u)=i\langle n|\partial_un\rangle = \frac{1}{2}\begin{pmatrix}
-1 & 0\\
0 & -1
\end{pmatrix}
$$

路径走过一圈之后的演化:
$$
U_{geom}=exp(i\pi \sigma_z)=\begin{pmatrix}
-1 & 0\\
0 & 1
\end{pmatrix}
$$

* 路径穿过原点
* 能量接近 0
* 几何相主导
这个Z gate的相位门完全来自于路径而不是某个固定的hamiltonian



Berry 连接的定义与几何相（修正）：

$$
A(u)=iW^\dagger(u)\partial_u W(u),
$$

其中 $W(u)=(|+(u)\rangle,|-(u)\rangle)$ 为瞬时本征向量矩阵。演化的几何因子写为

$$
U_{\rm geom}=\mathcal P\exp\Big(i\int_0^{2\pi} A(u)\,du\Big).
$$

对两能级有效哈密顿 $H_{\rm eff}=\vec d(u)\cdot\vec\sigma$，几何相等于 Bloch 球上被路径包围的立体角的一半：

$$
\gamma_\pm=\mp\frac{\Omega}{2}=\mp\frac{1}{2}\int_0^{2\pi}\bigl(1-\cos\theta(u)\bigr)\,du,
$$

其中 $\theta(u)=\arctan\frac{\sqrt{d_x^2+d_y^2}}{d_z}$. 对本模型可写为：

$$
\gamma=\pi\left(1-\frac{\delta}{\sqrt{1+\delta^2}}\right),\qquad
\phi_{\rm dyn}=2\pi\sqrt{1+\delta^2}.
$$

注意：局部 gauge 可使得 $A_{nn}(u)$ 在连续选取下为 0，但若本征向量在 $u\to u+2\pi$ 时返回到其相反向量（例如含半角相位的表示），则存在额外的单值性相位（monodromy），这部分不能只由积分 $\int A$ 捕获，需要把初末态重叠相位 $\arg\langle n(0)|n(2\pi)\rangle$ 一并考虑。

因此对 $\delta=0$，有 $\gamma=\pi$，每个本征态获得的几何相为 $\pm\pi$（符号由本征态的取向决定），在本征基下应写作

$$
U_{\rm geom}=\mathrm{diag}(e^{i\gamma_+},e^{i\gamma_-}),
$$

对 $\delta=0$ 可取為 $\mathrm{diag}(-1,-1)=-I$（此为全局相位，不影响测量），因此此前将 $A(u)$ 写为 $\tfrac12\mathrm{diag}(-1,-1)$ 并断言 $U_{\rm geom}=\exp(i\pi\sigma_z)=\mathrm{diag}(-1,1)$ 是不一致的：后者对应的是对两本征态赋予不同相位 $\pi$ 与 $0$，与 Bloch 球/立体角计算结果不符。



## 情况 2：ABS（耦合）

$$
\delta \neq 0
$$

* 路径远离原点
* 出现能隙
* 动力学相主导



## 几何图像

| 状态  | 路径   |
|-  | - |
| MZM | 围绕原点 |
| ABS | 偏移圆  |


在超导量子比特（transmon）:XX,YY：通过耦合器调节,相位 u：通过驱动相位控制

在纳米线 / 超导结构:XX,YY：对应配对 + 隧穿,u：对应超导相位差

实验上就是：调节耦合强度 + 相位，沿路径走一圈

Pauli 张量基让“拓扑编织”变成“可编程控制路径”，从而可以在普通量子系统中实现类拓扑的非阿贝尔操作。

不需要：真正的 Majorana\真正的拓扑态;只需要：Pauli Hamiltonian+一条闭路径,就能实现非阿贝尔几何操作

# 七、Berry 相与动力学相

## Berry 相

$$
\gamma = \pi\left(1 - \frac{\delta}{\sqrt{1+\delta^2}}\right)
$$


## 动力学相

$$
\phi_{\text{dyn}} = 2\pi\sqrt{1+\delta^2}
$$

## 判据

$$
\mathcal{Q} = \frac{\gamma}{\phi_{\text{dyn}}}
$$


| 区域                 | 物理  |
| ------ |     ---         |
| $\mathcal{Q} \gg 1$  | MZM |
| $\mathcal{Q} \sim 1$ | 过渡  |
| $\mathcal{Q} \ll 1$  | ABS |



# 八、非阿贝尔操作（合成 braid）



## 最小模型(XX,YY,XY)
这里定义
$$
H(u)= J[\cos u,XX + \sin u,YY] + \epsilon XY
$$

## 子空间有效形式

$$
H_{\text{eff}}(u)=
\begin{pmatrix}
0 & J e^{-iu}+i\epsilon \\
J e^{iu}-i\epsilon & 0
\end{pmatrix}
$$


## 闭路径演化

$$
U=\mathcal{T}\exp\left(-i\int_0^{2\pi} H(u)du\right)
$$


## 得到门

$$
U_{\text{braid}} \approx
\exp\left(-i\frac{\pi}{4}\sigma_y\right)
$$

# 九、控制电路设计

## 三段路径

### 段 A

$$
u:0 \rightarrow \pi/2 \\
H_A(u)= \cos u,XX + \sin u,YY + \epsilon XY
$$

### 段 B

$$
u:\pi/2 \rightarrow \pi \\
H_B(u)= -\sin u,XX + \cos u,YY + \epsilon XY
$$

### 段 C

$$
u:\pi \rightarrow 2\pi\\
H_C(u)= -\cos u,XX - \sin u,YY + \epsilon XY
$$



## 总演化

$$
U = U_C U_B U_A
$$
在绝热（或子空间隔离良好）下，可以写成
$$
U \approx  \underbrace{\mathcal P exp(\int A du)}_{\text{geom,non-abel}}  \cdot \underbrace{exp(-i \int E du)}_{\text{dyn}}
$$


# 十、实验实现对应



## 可控量

| Pauli 项 | 实验控制 |
| - | - |
| XX, YY  | 耦合器  |
| XY      | 相位偏置 |
| u       | 驱动相位 |



## 操作流程

1. 设置耦合强度
2. 扫描相位 (u(t))
3. 按三段路径执行
4. 完成闭环




实验实现映射（可操作）

以两类平台为例：

1）超导量子比特（transmon）
XX,YY：通过可调耦合器（tunable coupler）或双驱动实现;
u(t)：驱动相位（IQ 调制）线性扫动;
ϵXY：通过相位偏置的交叉驱动产生;
步骤：设定三段脉冲包络与相位 → 顺序执行 A→B→C

2）半导体纳米线/超导杂化结构
XX,YY：对应有效隧穿+配对通道;
u：两端超导相位差;
ϵ：不对称耦合/相位偏置;
步骤：调相位差走闭环，同时维持子空间隔离




# conclusion

## 1. 可设计性

直接设计路径即可得到目标门



## 2. 非阿贝尔可控

通过 Pauli 系数控制



* MZM：几何主导
* ABS：动力学主导


