# ybe225: 全链 Hamiltonian 与 MZM 讨论

## 1. 正确方向：先构造全链 Hamiltonian

对于完整的 Majorana / eight-vertex 链模型，正确的数值构造顺序是：

1. 先把所有局域项和耦合项加起来，得到全链 Hamiltonian：
   $$
   H_{\rm total}(t) = H_1(t) + H_2(t) + \cdots + H_{\rm int}(t).
   $$

2. 再用这个全链 Hamiltonian 求时间演化算子：
   $$
   R_{\rm total} = \mathcal T \exp\left(-i\int H_{\rm total}(t)\,dt\right).
   $$

这种方式才是真正描述整个系统随时间演化的方式。它包含了：

- 局域项之间的耦合
- 同一时间点上不同项的相互干涉
- 全链的能量级结构与拓扑性质

如果把演化写成 $R_1 \otimes R_2 \otimes \cdots$，那只是完全解耦子系统的情形。
只有当不同块之间**没有耦合**、且各自 Hamiltonian 互相对易时，这个分解才成立。

---

## 2. 为什么要用 $H_{\rm total}$ 才能讨论 MZM

MZM 是一种全局拓扑零模，它的存在依赖于整体的 BdG 结构，而不是某个局部子空间的独立演化。

具体来说，构造 MZM 需满足：

- 全链有拓扑非平庸的 bulk gap；
- 开链存在局域的零能边缘态；
- 零能态随链长的分裂呈指数小；
- 对应的 BdG Hamiltonian 具有合适的粒-空对称性与拓扑不变量。

这些性质都是从 $H_{\rm total}$ 出发才能获得。
因此，正确方向应当是：

- 写出全链 Hamiltonian
- 对整个链求谱、求态、算拓扑指标
- 再讨论零能边缘态是否存在

---

## 3. 全链演化能否在子空间找到类似结论？

答案是：可以获得类似“几何 / 劈裂”的直觉结论，但不能直接等价于全局 MZM。

### 3.1 低能投影的作用

把全链 Hamiltonian 投影到某个低能子空间，得到的 $H_{\rm eff}$ 只是对子空间内的有效两能级模型：

$$
H_{\rm eff} = d_0 I + d_x\sigma_x + d_y\sigma_y + d_z\sigma_z.
$$

这个模型能告诉我们：

- 这对逻辑态是否近似简并；
- 这对逻辑态的演化是否主要由几何旋转主导；
- 这对逻辑态是否被 $d_z$ 型偏置劈裂。

这对判断“逻辑子空间演化是否像 MZM-like”很有帮助。

### 3.2 但它不是全局 MZM 判据

全局 MZM 还要求：

- 这个子空间要在全链拓扑结构下自然出现；
- 零能态要是全链的边缘态，而不是某个局域子空间的“近似零能”态；
- 子空间投影后的 $d_z \neq 0$ 不能直接说明全局拓扑性。

因此全链演化确实可以在子空间里得到类似的“几何/劈裂”结论，但这只是一个局部有效描述，不等同于真正的 MZM 证明。

---

## 4. eight-vertex 模型能否得到 MZM？

对于你当前使用的 eight-vertex 投影模型，关键问题是：

- 投影后的 $\vec d(u)$ 轨迹是否存在零点；
- 投影后的 $H_{\rm eff}$ 是否具有严格的零能态；
- 全链 BdG 是否是真正的拓扑非平庸相。

在你给出的示例中，

$$
\vec d(u) = (\cos u,\ \sin u,\ \delta),
$$

它的模是常数：

$$
|\vec d| = \sqrt{1+\delta^2}.
$$

这意味着：

- 投影后的两能级本征值始终为 $\pm\sqrt{1+\delta^2}$；
- 子空间内永远没有严格的 $E=0$；
- 因此这个投影模型本身不能直接给出一个零能子空间。

### 4.1 这是否说明全链一定没有 MZM？

不是。

你可以把它理解为：

- 这个 eight-vertex 投影模型没有把“零能 Majorana 边缘态”直接做成一个子空间的零能态；
- 它更像是一个“几何门 / 逻辑子空间”模型；
- 但全链是否真的具有 MZM，还需要检查 $H_{\rm total}$ 的全局拓扑性质。

### 4.2 如果要得到全局 MZM，应该怎样改模型？

需要保证：

- 全链 Hamiltonian 的 BdG 形式在某些参数区域为拓扑非平庸；
- 开链系统有边缘态，且零能态对称保护；
- 子空间投影不破坏这一拓扑结构。

也就是说，eight-vertex 的局部投影若没有给出 $E=0$，说明它可能只是在描述“几何门的逻辑态”，而不是“真实的零能边缘 Majorana”。

---

## 5. 建议性的分析步骤

如果你要判断一个具体模型是否能得到 MZM：

1. 先写出完整的全链 BdG Hamiltonian $H_{\rm total}$；
2. 计算全链谱，检查是否存在零能态；
3. 检查开链边缘态是否局域于边缘并随链长指数退化；
4. 计算拓扑不变量或 bulk gap；
5. 再把低能子空间投影结果作为补充，解释逻辑态演化的几何/动力学性质。

---

## 6. 结论

- 构建全链时，应优先采用 $H_{\rm total}=\sum H_i$，再求整体演化；
- 子空间投影可以给出“几何 vs 劈裂”的直觉，但不能代替全局 MZM 判定；
- eight-vertex 投影模型如果没有给出 $E=0$，说明它不是一个直接的“零能 MZM 投影模型”，而更像是一个“逻辑门 / 低能子空间近似模型”。

## 7. 一个合适的路径积分例子：瞬时 $E\neq 0$，但净演化可回到零相位

这里要区分两件事：

- **瞬时本征能量** $E(t)$：某一时刻 Hamiltonian 的本征值，通常不为零；
- **沿路径累积的动力学相**：
   $$
   \phi_{\rm dyn} = -\int_0^T E(t)\,dt.
   $$

因此，“全局为零”通常不是指 $E(t)$ 在每一时刻都为零，而是指**整个演化算子在一个周期后只剩下全局相位，或者动力学相在一段闭合路径后被抵消**。

### 7.1 最简单的抵消例子

取一个两段路径：

$$
H_{\rm eff}(t)=
\begin{cases}
 +E\,\sigma_z, & 0\le t< T/2,\\
 -E\,\sigma_z, & T/2\le t\le T,
\end{cases}
$$

其中 $E>0$ 是常数。显然每一时刻的瞬时能级都是 $\pm E$，并不为零。可是总演化为

$$
U(T)=e^{-i(+E\sigma_z)T/2}\,e^{-i(-E\sigma_z)T/2}=I.
$$

也就是说：

- 每一段路径的瞬时能量都不为零；
- 但两段路径的动力学相完全相反，彼此抵消；
- 最终总演化回到单位算子（或只差一个全局相位）。

这就是“瞬时有能量，路径积分后净效果为零”的最直接例子。

### 7.2 写成路径参数 $u(t)$ 的形式

如果把控制写成参数路径 $u(t)$，则可以构造一条前半段和后半段符号相反的路径，例如：

$$
H_{\rm eff}[u(t)] = f(u(t))\,\sigma_z,
$$

并令

$$
f(u(t))=
\begin{cases}
 +E, & 0\le t< T/2,\\
 -E, & T/2\le t\le T.
\end{cases}
$$

这样虽然每一时刻 $|f(u(t))|=E\neq 0$，但积分满足

$$
\int_0^T f(u(t))\,dt = 0,
$$

于是净动力学相消失。若再配合闭合路径的 Berry 相，就可能得到一个纯几何门。

### 7.3 对 eight-vertex 例子的含义

对于你当前的 eight-vertex 示例，

$$
\vec d(u)=(\cos u,\sin u,\delta),\qquad |\vec d|=\sqrt{1+\delta^2},
$$

如果只是在同一条路径上单向推进，动力学相是始终累积的，**不会自动积分成零**。要想抵消它，必须额外安排：

- 反向段使有效 Hamiltonian 变号；或
- 通过分段控制让不同区间的动力学相互抵消；或
- 通过选择演化时间使动力学相只差一个 $2\pi$ 的整数倍，从而在门层面等效为全局相位。

所以，eight-vertex 路径本身若没有显式的“符号翻转段”或“补偿段”，就不能指望由 $u(t)$ 的简单积分自动得到 $E=0$。

### 7.4 一个真正可积零的 braid 回路

如果目标是让**净演化中的动力学部分抵消**，最直接的做法不是只走一条单向路径，而是走一条“正向 + 反向”的闭合回路。对于 eight-vertex 例子，可以取

$$
u(t)=
\begin{cases}
\omega t, & 0\le t\le T/2,\\
\omega(T-t), & T/2\le t\le T,
\end{cases}
$$

并让第二段执行第一段的逆演化，即

$$
U(T)=U_-(T/2)\,U_+(T/2),\qquad U_-(T/2)=U_+(T/2)^{-1}.
$$

这里每一段的瞬时本征能量仍然是

$$
E_\pm(u)=\pm\sqrt{1+\delta^2},
$$

并没有在任一时刻真正变成零；但是因为第二段是第一段的逆路径，路径有序指数满足

$$
U(T)=I,
$$

因此净动力学相（以及由 $H$ 生成的总作用）抵消了。

这正是对 eight-vertex / braid 路径最合适的理解：

- **瞬时能谱不必为零**；
- **净演化可以为零（或只差全局相位）**；
- 需要的是闭合回路或逆路径，而不是单向推进的一段 open path。

如果你愿意，我可以继续把这段讨论整理成 `ybe225.md` 的一个“全链 vs 投影模型”的对照小节。