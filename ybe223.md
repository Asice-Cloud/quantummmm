# Pauli 张量基 → 有效两能级模型 → MZM/ABS 与轨迹几何（完整整理）

---

# 一、目标

本文整理以下核心内容：

1. 从两比特 Hamiltonian 投影到有效模型
2. 构建 $H_{\text{eff}}=\vec d\cdot \vec\sigma$
3. 给出完整 **Pauli → $\vec d$** 映射字典
4. 分析 eight-vertex 示例
5. 用轨迹几何统一描述 **MZM 与 ABS**


不是某个 Pauli 项本身决定 MZM 或 ABS，而是：是否存在一个（近）零能、受保护的简并子空间

更具体一点：

MZM-like：存在近零能简并,子空间内主要是“旋转（off-diagonal）”,几何主导

ABS：简并被打破,出现有限能隙,动力学主导

---

# 二、从两比特到有效两能级模型


## 2.1 原始 Hamiltonian

$$
H(u)=\sum_{\alpha,\beta} h_{\alpha\beta}(u)\sigma^\alpha\otimes\sigma^\beta
$$

这是一个 $4\times4$ 系统。

---

## 2.2 选择低能子空间

$$
\mathcal S={|01\rangle,\ |10\rangle}
$$

定义投影：

$$
P = |01\rangle\langle01| + |10\rangle\langle10|
$$

物理原因

这个子空间通常对应：单粒子子空间（占据数=1）或 Majorana 编码子空间

---

## 2.3 有效 Hamiltonian

$$
\boxed{
H_{\text{eff}} = P H P
}
$$


# 三 形式
*为什么一定可以写成* 
$\vec d\cdot \vec\sigma$


## 关键定理

任意 $2\times2$ 厄米矩阵：

$$
H = d_0 I + d_x\sigma_x + d_y\sigma_y + d_z\sigma_z
$$
这是来源于SU(2) 基底完备性

---

因此：

$$
\boxed{
H_{\text{eff}} = d_0 I + \vec d\cdot \vec\sigma
}
$$

---

## 系数计算

$$
d_i = \frac{1}{2}\mathrm{Tr}(H_{\text{eff}}\sigma_i)
$$

$$
d_0 = \frac{1}{2}\mathrm{Tr}(H_{\text{eff}})
$$

---

---

# 四、完整 Pauli 投影字典

子空间基底：$|01\rangle , |10\rangle$

---

## 4.1 投影为 0

$$
\begin{aligned}
&I\otimes X,\ I\otimes Y,\ X\otimes I,\ Y\otimes I \
&X\otimes Z,\ Z\otimes X,\ Y\otimes Z,\ Z\otimes Y
\end{aligned}
$$

---

## 4.2 投影为对角项

| 项            | 投影          |
| ------------ | ----------- |
| $I\otimes I$ | $I$         |
| $Z\otimes Z$ | $-I$        |
| $Z\otimes I$ | $+\sigma_z$ |
| $I\otimes Z$ | $-\sigma_z$ |

---

## 4.3 投影为非对角项

| 项            | 投影          |
| ------------ | ----------- |
| $X\otimes X$ | $+\sigma_x$ |
| $Y\otimes Y$ | $+\sigma_x$ |
| $X\otimes Y$ | $-\sigma_y$ |
| $Y\otimes X$ | $+\sigma_y$ |

---

### 完整逐步计算（基与矩阵元）

下面给出在基序 $\{|00\rangle,|01\rangle,|10\rangle,|11\rangle\}$ 下的显式计算，并把子空间选为

$$
|\tilde0\rangle := |01\rangle = \begin{pmatrix}0\\1\\0\\0\end{pmatrix},\qquad
|\tilde1\rangle := |10\rangle = \begin{pmatrix}0\\0\\1\\0\end{pmatrix}.
$$

Pauli 矩阵取为
$$
X=\begin{pmatrix}0&1\\1&0\end{pmatrix},\quad
Y=\begin{pmatrix}0&-i\\i&0\end{pmatrix},\quad
Z=\begin{pmatrix}1&0\\0&-1\end{pmatrix}.
$$

对每个张量积算符 $O=A\otimes B$，在子空间 $\{|\tilde0\rangle,|\tilde1\rangle\}$ 的有效矩阵元素为
$$
M_{\alpha\beta}=\langle\widetilde\alpha|\,O\,|\widetilde\beta\rangle,
\qquad \alpha,\beta\in\{0,1\}.
$$

计算要点：对基向量作用并写回子空间坐标。

1) $X\otimes X$：

$$
\begin{aligned}
(X\otimes X)|01\rangle &= (X|0\rangle)\otimes(X|1\rangle)=|1\rangle\otimes|0\rangle=|10\rangle,\\
(X\otimes X)|10\rangle &= |01\rangle.
\end{aligned}
$$

因此在子空间表示为

$$
M_{XX}=\begin{pmatrix}0 & 1\\1 & 0\end{pmatrix}=\sigma_x.
$$

2) $Y\otimes Y$：利用 $Y|0\rangle = i|1\rangle$, $Y|1\rangle = -i|0\rangle$，

$$
\begin{aligned}
(Y\otimes Y)|01\rangle &= (i|1\rangle)\otimes(-i|0\rangle) = (+1) |10\rangle,\\
(Y\otimes Y)|10\rangle &= (+1) |01\rangle.
\end{aligned}
$$

因此
$$
M_{YY}=\begin{pmatrix}0 & 1\\1 & 0\end{pmatrix}=\sigma_x.
$$

3) $X\otimes Y$：

$$
\begin{aligned}
(X\otimes Y)|01\rangle &= (X|0\rangle)\otimes(Y|1\rangle)=|1\rangle\otimes(-i|0\rangle) = -i|10\rangle,\\
(X\otimes Y)|10\rangle &= (X|1\rangle)\otimes(Y|0\rangle)=|0\rangle\otimes(i|1\rangle) = i|01\rangle.
\end{aligned}
$$

因此
$$
M_{XY}=\begin{pmatrix}0 & i\\-i & 0\end{pmatrix} = -\sigma_y.
$$

4) $Y\otimes X$：

$$
\begin{aligned}
(Y\otimes X)|01\rangle &= (i|1\rangle)\otimes|0\rangle = i|10\rangle,\\
(Y\otimes X)|10\rangle &= (-i|0\rangle)\otimes|1\rangle = -i|01\rangle.
\end{aligned}
$$

因此
$$
M_{YX}=\begin{pmatrix}0 & -i\\i & 0\end{pmatrix} = +\sigma_y.
$$

5) 验证（$Z$ 项）：

$$
(Z\otimes I)|01\rangle = (+1)|01\rangle,\qquad (Z\otimes I)|10\rangle = -1\,|10\rangle
$$
所以 $Z\otimes I\to \mathrm{diag}(+1,-1)=\sigma_z$. 而
$$
(I\otimes Z)|01\rangle = -|01\rangle,\qquad (I\otimes Z)|10\rangle = +|10\rangle
$$
所以 $I\otimes Z\to -\sigma_z$, 且 $Z\otimes Z\to -I$。

总结映射（在子空间 $\{|01\rangle,|10\rangle\}$ 下）

$$
X\otimes X \mapsto +\sigma_x,\\
Y\otimes Y \mapsto +\sigma_x,\\
X\otimes Y \mapsto -\sigma_y,\\
Y\otimes X \mapsto +\sigma_y,\\
Z\otimes I \mapsto +\sigma_z,\\
I\otimes Z \mapsto -\sigma_z,\\
 Z\otimes Z \mapsto -I
$$


注：上述结论依赖于所选的基序与相位约定（这里采用标准基顺序及本征向量的惯常相位），若改变基序或对本征向量做非平凡局部相位变换，矩阵表示会相应改变。



# 五、最终映射
如果有
$$
H = a\,XX + b\,YY + c\,XY + d\,ZZ
$$
投影到子空间（基取为 $\{|01\rangle,|10\rangle\}$）后得到的等价表示为
$$
H_{\mathrm{eff}} = (a+b)\,\sigma_x - c\,\sigma_y + d_0\,I,
$$
更一般地，对任意系数 $h_{\alpha\beta}$ 有：
$$
\boxed{
\begin{aligned}
d_x(u) &= h_{xx}(u) + h_{yy}(u) \\
d_y(u) &= -h_{xy}(u) + h_{yx}(u) \\
d_z(u) &= h_{zI}(u) - h_{Iz}(u) \\
d_0(u) &= h_{II}(u) - h_{zz}(u)
\end{aligned}
}
$$

---

## 物理意义

MZM/ABS 由 $(d_x​,d_y​,d_z​)$ 的结构决定

| 分量        | 作用           |
| --------- | ------------ |
| $d_x,d_y$ | mixing（几何旋转） |
| $d_z$     | 能级劈裂（ABS 来源） |


对照

|分量	|来源	|物理 |
|---|---|--|
|d_x |XX, YY	|hopping / pairing
|d_y|XY, YX	|相位 / chirality
|d_z|ZI, IZ	|能量不对称 / 重叠

这个投影字典说明：每一个 Pauli 项在低能子空间中“变成哪一个方向的有效磁场”，从而直接决定它是在“产生非阿贝尔几何”，还是在“破坏拓扑简并”


---

---

# 六、Eight-Vertex 示例

---

## 6.1 R 矩阵

$$
R(u)=
\begin{pmatrix}
\cos u & 0 & 0 & i\delta \sin u \\
0 & \sin u & \cos u & 0 \\
0 & \cos u & \sin u & 0 \\
i\delta \sin u & 0 & 0 & \cos u
\end{pmatrix}
$$

---

## 6.2 得到 Hamiltonian

$$
H(u)\approx \cos u\, XX + \frac{\sin u}{2}\,\bigl(YX - XY\bigr) + \frac{\delta}{2}\,\bigl(Z\otimes I - I\otimes Z\bigr)
$$

矩阵形式（在 {|01>,|10>} 基下）:
$$
H_{eff} = \begin{pmatrix}
δ, \cos u − i sin u \\  cos u + i \sin u, −δ 
\end{pmatrix} = \begin{pmatrix}
δ &, e^{-iu}\\e^{iu} &, −δ
\end{pmatrix}
$$
 

瞬时能级d的本征值：
$$
E = ±\sqrt{cos^2 u + sin^2 u + δ^2} = ±\sqrt{1 + δ^2}
$$


## 6.3 映射到 $\vec d$

$$
\boxed{\vec d(u) = (\cos u,\ \sin u,\ \delta)}
$$

---

# 七、轨迹几何解释

---

## 7.1 理想情况（MZM-like）

$$
\delta = 0
$$

$$
\vec d(u) = (\cos u,\sin u,\ 0)
$$
---

👉 轨迹：

* 单位圆
* 围绕原点

---

## 7.2 耦合情况（ABS）
$$
\delta \neq 0
$$

$$
\vec d(u) = (\cos u,\sin u, \delta)
$$
---

👉 轨迹：

* 抬高的圆（纬度圈）
* 不包围原点

---

---

# 八、统一判据

---

## 8.1 路径是否包围原点
$$
\boxed{
\text{MZM} \Longleftrightarrow \vec d(u)\ \text{绕原点}
}
$$
---

## 8.2 能隙
$$
E = \pm |\vec d|
$$

---

## 8.3 几何 vs 动力学

| 区域  | 特征    |
| --- | ----- |
| MZM | 几何主导  |
| ABS | 动力学主导 |

---

---

# 九、关键物理理解

---

## 🔥 最核心关系

| Pauli 项        | 作用     |
| -------------- | ------ |
| XX, YY, XY, YX | 产生几何旋转 |
| ZI, IZ         | 产生能级劈裂 |

---

---

## 🔥 本质结论

> **MZM/ABS 不由某个 Pauli 项决定，而由 $\vec d$ 的结构决定**

---

---

# 十、最终总结

---

> **整个问题可以完全转化为：**
>
> $$
> \boxed{
> \text{两比特系统} ;\longrightarrow; \text{Bloch 球上的一条路径}
> }
> $$
>
> * MZM：路径绕原点
> * ABS：路径远离原点
> * 非阿贝尔性：路径不对易

---
对应关系
|数学	|物理|
|-|-|
|$\vec d(u)$	|有效磁场|
|eigenstate	|自旋方向
|Berry 相|	磁单极
|路径	|自旋轨迹

把问题从：4×4 多体系统变成Bloch 球上的一条曲线




---

# 十一、后续可做工作

---

1. 画出 $\vec d(u)$ 三维轨迹
2. 计算 Berry 相
3. 分析 YBE 偏离
4. 优化路径实现稳定量子门
